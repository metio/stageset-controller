<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Security &amp; multi-tenancy on StageSet Controller</title><link href="https://stageset.projects.metio.wtf/security/" rel="alternate" type="text/html" title="html"/><link href="https://stageset.projects.metio.wtf/security/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://stageset.projects.metio.wtf/security/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-23T07:22:19+00:00</updated><author><name>metio.wtf</name></author><id>https://stageset.projects.metio.wtf/security/</id><entry><title type="html">Admission webhook</title><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/security/image-verification/?utm_source=atom_feed" rel="related" type="text/html" title="Image verification"/><link href="https://stageset.projects.metio.wtf/runbooks/imageunverified/?utm_source=atom_feed" rel="related" type="text/html" title="ImageUnverified"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotverified/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotVerified"/><link href="https://stageset.projects.metio.wtf/security/network-policy/?utm_source=atom_feed" rel="related" type="text/html" title="Network policy"/><id>https://stageset.projects.metio.wtf/security/admission-webhook/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The validating webhook for StageSet — the invariants it enforces, why it exists instead of CEL, the failure-policy trade-off, and the two TLS provisioning modes.</blockquote><p>A StageSet&rsquo;s spec carries invariants the CRD&rsquo;s structural schema cannot enforce
cheaply, so the controller ships a <strong>validating admission webhook</strong> that rejects a
bad StageSet at <code>kubectl apply</code> time — on <code>create</code> and <code>update</code> of
<code>stages.metio.wtf/v1</code> StageSets — before it ever reaches a reconcile.</p>
<h2 id="why-a-webhook-and-not-cel">Why a webhook and not CEL</h2>
<p>The natural home for these rules is a CRD CEL validation. The catch: CEL cost is
multiplied by the size of every enclosing array, and a StageSet&rsquo;s <code>spec.stages</code> and
each stage&rsquo;s action lists are deliberately <strong>unbounded</strong>. A CEL rule over them
makes the apiserver reject the CRD on cost grounds. Moving the checks into a webhook
keeps the lists unbounded while still validating them.</p>
<h2 id="what-it-validates">What it validates</h2>
<p>A single <code>ValidateSpec</code> routine is the source of truth. It enforces:</p>
<ul>
<li><strong>One operation per action</strong> — every entry in <code>actions.pre</code>/<code>post</code>/<code>onFailure</code>
(and a migration&rsquo;s actions) sets exactly one of <code>patch</code> / <code>http</code> / <code>wait</code> /
<code>job</code> / <code>delete</code> / <code>apply</code>. Zero or two is rejected. See <a href="/defining-a-release/actions/">actions</a>
.</li>
<li><strong>One version source</strong> — when <code>spec.version</code> is set, exactly one of <code>value</code>,
<code>fromObject</code>, or <code>fromArtifact</code> (see <a href="/gating/versioned-migrations/">versioned migrations</a>
).</li>
<li><strong>Coherent migrations</strong> — <code>spec.migrations</code> require a <code>spec.version</code>, each
anchors to a real stage, and each migration action sets exactly one verb.</li>
<li><strong>Decryption shape</strong> — <code>spec.decryption</code>, when set, names the only supported
provider (<code>sops</code>), and a given <code>secretRef</code> carries a <code>name</code> (see
<a href="/security/encryption/">secrets encryption</a>
).</li>
<li>Reserved post-v1 fields are rejected up front.</li>
</ul>
<p>The same <code>ValidateSpec</code> also runs <strong>inside the reconciler as a fallback</strong>, so a
StageSet that slips past a disabled or bypassed webhook still fails loudly — it just
fails at reconcile (<code>Ready=False</code>) instead of at admission.</p>
<h2 id="failure-policy-trade-off">Failure-policy trade-off</h2>
<p>The webhook is registered with <code>failurePolicy: Fail</code>: if its endpoint is
unreachable, the apiserver <strong>rejects</strong> StageSet create/update rather than admit an
unvalidated object. During a rolling update that window is brief — the next replica
serves admission. If your GitOps tooling cannot tolerate even that, the
reconciler-side fallback lets you run with the webhook <strong>disabled</strong>
(<code>webhook.enabled: false</code>) and still catch invalid specs; they fail at reconcile
instead of at admission.</p>
<h2 id="tls-provisioning">TLS provisioning</h2>
<p>The apiserver dials the webhook over TLS and verifies the serving certificate
against the <code>caBundle</code> on the <code>ValidatingWebhookConfiguration</code>. Choose how that is
provisioned with <code>--webhook-cert-mode</code> (<code>cert-manager</code> or <code>self-signed</code>; the binary
defaults to <code>cert-manager</code>, while the chart value <code>webhook.certMode</code> defaults to
<code>self-signed</code> for a no-prerequisite install):</p>
<h3 id="cert-manager">cert-manager</h3>
<p>The chart renders a <code>cert-manager.io/v1</code> Certificate; cert-manager issues the
Secret, it is mounted at <code>--webhook-cert-dir</code>, and cert-manager renews it. The
webhook server hot-reloads the rotated files. Requires
<a href="https://cert-manager.io/">cert-manager</a>
 in the cluster.</p>
<h3 id="self-signed-chart-default">self-signed (chart default)</h3>
<p>No cert-manager dependency: the controller generates an ECDSA CA and serving
certificate in-pod, writes them to <code>--webhook-cert-dir</code>, and patches the named
<code>ValidatingWebhookConfiguration</code>&rsquo;s <code>caBundle</code> so the apiserver trusts the chain
(<code>--webhook-validating-config-name</code> is required). The <code>caBundle</code> is treated as a CA
<strong>set</strong> — each replica unions its own CA in and prunes only its own expired blocks,
so several replicas converge during a rolling update instead of clobbering each
other&rsquo;s trust. A renewer goroutine regenerates at <code>validity/3</code>
(<code>--webhook-cert-validity</code>, default one year) and the server hot-reloads without a
restart.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Helm values</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certMode</span><span class="p">:</span><span class="w"> </span><span class="l">self-signed     </span><span class="w"> </span><span class="c"># chart default; or cert-manager</span><span class="w">
</span></span></span></code></pre></div><p>The <a href="/reference/configuration/">configuration reference</a>
 lists the full
<code>--webhook-*</code> flag set.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/webhook" term="webhook" label="webhook"/><category scheme="https://stageset.projects.metio.wtf/tags/admission" term="admission" label="admission"/><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/><category scheme="https://stageset.projects.metio.wtf/tags/tls" term="tls" label="tls"/></entry><entry><title type="html">Image verification</title><link href="https://stageset.projects.metio.wtf/security/image-verification/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/imageunverified/?utm_source=atom_feed" rel="related" type="text/html" title="ImageUnverified"/><link href="https://stageset.projects.metio.wtf/reference/verifying-releases/?utm_source=atom_feed" rel="related" type="text/html" title="Verifying releases"/><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotverified/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotVerified"/><id>https://stageset.projects.metio.wtf/security/image-verification/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-07-19T11:01:24+02:00</updated><content type="html"><![CDATA[<blockquote>Gate a rollout on container image signatures — cluster ImageVerificationPolicies verify each image with cosign keyless identity and pin it to its digest before a stage applies it.</blockquote><p>Require every container image a rollout deploys to be signed before it reaches the
cluster. You write a cluster-scoped <code>ImageVerificationPolicy</code> that names the images it
governs and the identities allowed to sign them; the controller verifies each image a
stage renders and holds the stage if any image fails — nothing unverified is applied.</p>
<p>Verification is owned by the platform, not the tenant: a <code>StageSet</code> author deploys
without declaring any signing configuration, and the policies that gate their images
live in one cluster-scoped resource an administrator controls.</p>
<h2 id="write-a-policy">Write a policy</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">stages.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ImageVerificationPolicy</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">acme-images</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">images</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="s2">&#34;ghcr.io/acme/**&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">authorities</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">keyless</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">issuer</span><span class="p">:</span><span class="w"> </span><span class="l">https://token.actions.githubusercontent.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">subjectRegExp</span><span class="p">:</span><span class="w"> </span><span class="l">^https://github\.com/acme/.+/\.github/workflows/release\.yml@refs/heads/main$</span><span class="w">
</span></span></span></code></pre></div><p>Every image whose repository matches an <code>images</code> glob is governed by the policy. A
governed image must carry a Sigstore signature from <strong>at least one</strong> listed authority.
The globs match the ref without its tag or digest: <code>*</code> spans one path segment, <code>**</code>
spans any number, so <code>ghcr.io/acme/**</code> covers <code>ghcr.io/acme/api</code> and
<code>ghcr.io/acme/team/worker</code> alike.</p>
<p>An authority is one accepted signer — set exactly one of <code>keyless</code> or <code>key</code>:</p>
<ul>
<li>
<p><strong>keyless</strong> — the Fulcio certificate a <a href="https://docs.sigstore.dev/">cosign</a>
 keyless
signature carries. Match the OIDC issuer with <code>issuer</code> (exact) or <code>issuerRegExp</code>, and
the certificate subject with <code>subject</code> (exact) or <code>subjectRegExp</code>; set exactly one
form of each.</p>
</li>
<li>
<p><strong>key</strong> — a cosign public key, carried inline. The key is public, so it lives in the
policy itself — no Secret, and the controller needs no secret-read privilege:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">images</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="s2">&#34;registry.internal/**&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">authorities</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">key</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">publicKey</span><span class="p">:</span><span class="w"> </span><span class="p">|</span><span class="sd">
</span></span></span><span class="line"><span class="cl"><span class="sd">          -----BEGIN PUBLIC KEY-----
</span></span></span><span class="line"><span class="cl"><span class="sd">          MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAE…
</span></span></span><span class="line"><span class="cl"><span class="sd">          -----END PUBLIC KEY-----</span><span class="w">
</span></span></span></code></pre></div><p>Paste the contents of the <code>cosign.pub</code> produced by <code>cosign generate-key-pair</code>. A
key signature is verified against the current time — the key does not expire, so
trust rests on the signature over the digest, as with <code>cosign verify --key</code>.</p>
</li>
</ul>
<h2 id="how-a-stage-is-gated">How a stage is gated</h2>
<p>Between rendering a stage&rsquo;s objects and applying them, the controller:</p>
<ol>
<li>Extracts every container image from the rendered pod specs (<code>Pod</code>, <code>Deployment</code>,
<code>StatefulSet</code>, <code>DaemonSet</code>, <code>ReplicaSet</code>, <code>Job</code>, <code>CronJob</code>).</li>
<li>Selects the policies whose globs match each image.</li>
<li>Resolves the image to its digest, fetches the Sigstore bundle cosign attaches as an
OCI referrer, and verifies the signature against the policy&rsquo;s authorities and the
resolved digest.</li>
<li>Rewrites the image in the manifest to <code>repository@sha256:…</code> — the exact digest it
verified — so a tag cannot be swapped between the check and the apply.</li>
</ol>
<p>If any governed image fails, the stage is held with <code>Ready=False</code> and reason
<code>ImageUnverified</code>, and later stages never run. See the
<a href="/runbooks/imageunverified/">ImageUnverified runbook</a>
 for the remediation steps.</p>
<h2 id="sign-images-so-they-verify">Sign images so they verify</h2>
<p>Images must carry a <strong>new-format</strong> Sigstore bundle, which cosign attaches to the image
as an OCI referrer:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">cosign sign --new-bundle-format ghcr.io/acme/api@sha256:…
</span></span></code></pre></div><p>An image without a bundle cannot be verified. Mirror and re-sign a third-party image
into your own registry, or exempt it with a <code>skip</code> glob:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">images</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="s2">&#34;ghcr.io/acme/**&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">skip</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="s2">&#34;ghcr.io/acme/vendored/**&#34;</span><span class="w">   </span><span class="c"># audited: a base image that cannot carry a bundle</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">authorities</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">keyless</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">issuer</span><span class="p">:</span><span class="w"> </span><span class="l">https://token.actions.githubusercontent.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">subjectRegExp</span><span class="p">:</span><span class="w"> </span><span class="l">^https://github\.com/acme/.+$</span><span class="w">
</span></span></span></code></pre></div><p>A skipped image is recorded, never silently passed.</p>
<h2 id="deny-images-no-policy-governs">Deny images no policy governs</h2>
<p>By default an image that matches no policy applies unchanged — verification covers
only what a policy names. Pass <code>--require-image-verification</code> to the controller to flip
that to deny-by-default: an image no policy governs holds its stage under
<code>ImageUnverified</code>, so a new registry cannot ship unverified because a policy for it was
forgotten.</p>
<h2 id="break-glass">Break glass</h2>
<p>A genuine emergency — a broken signing pipeline blocking a critical fix — can bypass
the gate for one reconcile by annotating the <code>StageSet</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl annotate stageset acme stages.metio.wtf/skip-image-verification<span class="o">=</span><span class="s2">&#34;pipeline outage, INC-1234&#34;</span>
</span></span></code></pre></div><p>The bypass records a <code>Warning</code> event naming the reason. It is an operator action; a
tenant editing their own <code>StageSet</code> should not hold this power.</p>
<h2 id="air-gapped-clusters">Air-gapped clusters</h2>
<p>The controller loads the public Sigstore trusted root over TUF on first use. In a
cluster without egress, point it at an offline trusted-root bundle:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">--image-verification-trusted-root=/etc/sigstore/trusted_root.json
</span></span></code></pre></div><p>An on-prem registry served over plain HTTP (no TLS) is fetched over HTTPS by default
and fails the handshake. List each such registry so its verification fetches use HTTP;
repeat the flag per registry:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">--image-verification-insecure-registry=registry.internal:5000
</span></span></code></pre></div><p>This governs only the verifier&rsquo;s bundle fetch, not the kubelet&rsquo;s image pulls.</p>
<h2 id="constraints">Constraints</h2>
<p>A <code>requireAttestations</code> entry is rejected by the <a href="/security/admission-webhook/">admission webhook</a>
:
signature verification is enforced, attestation-predicate verification is not, so a
policy never advertises a guarantee the controller does not keep.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/><category scheme="https://stageset.projects.metio.wtf/tags/supply-chain" term="supply-chain" label="supply-chain"/><category scheme="https://stageset.projects.metio.wtf/tags/images" term="images" label="images"/><category scheme="https://stageset.projects.metio.wtf/tags/cosign" term="cosign" label="cosign"/><category scheme="https://stageset.projects.metio.wtf/tags/sigstore" term="sigstore" label="sigstore"/></entry><entry><title type="html">Multi-cluster and tenancy</title><link href="https://stageset.projects.metio.wtf/security/multi-cluster/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/rbacdenied/?utm_source=atom_feed" rel="related" type="text/html" title="RBACDenied"/><id>https://stageset.projects.metio.wtf/security/multi-cluster/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-07-16T21:18:25+02:00</updated><content type="html"><![CDATA[<blockquote>Impersonation, watch scoping, single-tenant cluster-admin, and remote clusters.</blockquote><p>There are two ways to run the controller, and they map onto two different trust
models. Pick the one that matches your cluster:</p>
<ul>
<li><strong>Multi-tenant</strong> — the controller holds no write access of its own and applies
every <code>StageSet</code> as that <code>StageSet</code>&rsquo;s <code>serviceAccountName</code>. Each tenant&rsquo;s RBAC
bounds what its releases can touch. This is the chart default.</li>
<li><strong>Single-tenant</strong> — the cluster has one operator, so per-tenant isolation buys
nothing. Run the controller under its own identity bound to <code>cluster-admin</code> and
skip impersonation entirely — the model Flux&rsquo;s <code>helm-controller</code> uses in its
default install.</li>
</ul>
<p>The two sections below set each one up. The optional
<a href="#scoping-the-controller-to-a-namespace-set">watch scoping</a>
 narrows <em>which</em>
namespaces a multi-tenant controller sees.</p>
<h2 id="tenant-serviceaccounts-multi-tenant">Tenant ServiceAccounts (multi-tenant)</h2>
<p>The controller never applies your manifests as itself. Set <code>serviceAccountName</code>
and every operation for that <code>StageSet</code> — build, apply, prune, actions — runs as
that ServiceAccount. The <code>StageSet</code> can do exactly what the SA&rsquo;s RBAC permits, and
nothing more.</p>
<p>On the local cluster the controller assumes the tenant identity by minting a
short-lived <a href="https://kubernetes.io/docs/reference/kubernetes-api/authentication-resources/token-request-v1/">TokenRequest</a>

token for the named ServiceAccount and authenticating as it. That needs only
<code>create</code> on <code>serviceaccounts/token</code> in the controller&rsquo;s ClusterRole — the powerful
<code>impersonate</code> verb is not granted. The token is cached per ServiceAccount and
re-minted as it nears expiry.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer   </span><span class="w"> </span><span class="c"># all writes impersonate this SA</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app</span><span class="w">
</span></span></span></code></pre></div><p>Grant the SA only the rights that release needs:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">rbac.authorization.k8s.io/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">RoleBinding</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">roleRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">apiGroup</span><span class="p">:</span><span class="w"> </span><span class="l">rbac.authorization.k8s.io</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ClusterRole</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">edit</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">subjects</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ServiceAccount</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span></code></pre></div><p>This is the multi-tenancy model: isolation comes from each <code>StageSet</code> being bounded
by its tenant SA, not from the controller&rsquo;s own grant — by default the chart gives
the controller <code>create</code> on <code>serviceaccounts/token</code> and read access, no blanket write
and no <code>impersonate</code>. A <code>StageSet</code> with no <code>serviceAccountName</code>, or one bound to a
too-narrow SA, fails closed rather than escalating.</p>
<h3 id="per-stage-serviceaccounts">Per-stage ServiceAccounts</h3>
<p>A stage can set its own <code>serviceAccountName</code>, overriding the StageSet default for
that stage alone. Every cluster operation the stage performs — apply, prune,
readiness verification, actions, and rollback — runs as the stage&rsquo;s ServiceAccount,
bounded by its RBAC. This lets one ordered <code>StageSet</code> promote a change through
environments that each have their own identity, without splitting the rollout
across separate <code>StageSet</code>s:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">staging-deployer     </span><span class="w"> </span><span class="c"># default for stages that omit it</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">staging</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w"> </span>{<span class="w"> </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app }    </span><span class="w"> </span><span class="c"># runs as staging-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">production</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">prod-deployer     </span><span class="w"> </span><span class="c"># runs as prod-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w"> </span>{<span class="w"> </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app }</span><span class="w">
</span></span></span></code></pre></div><p>Each stage&rsquo;s identity is minted the same way as the StageSet default — a
short-lived TokenRequest token locally, or header impersonation against a remote
<code>kubeConfig</code>. Stages sharing a ServiceAccount share one cached token. SOPS
decryption keys are read under the StageSet-level <code>serviceAccountName</code>, since
<code>spec.decryption</code> is a StageSet-wide setting rather than a per-stage one.</p>
<h2 id="single-tenant-cluster-admin">Single-tenant cluster-admin</h2>
<p>On a cluster with a single operator, per-<code>StageSet</code> tenant identities are friction
with no payoff — there is no other tenant to isolate from. Run the controller the way
Flux&rsquo;s <code>helm-controller</code> runs by default: under its own ServiceAccount, bound to
the built-in <code>cluster-admin</code> ClusterRole. <code>StageSet</code>s then omit <code>serviceAccountName</code>
and apply as the controller, which can write any kind cluster-wide.</p>
<p>Turn it on with one Helm value:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">rbac</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">clusterAdmin</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">     </span><span class="c"># bind the controller SA to cluster-admin</span><span class="w">
</span></span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">helm --namespace stageset-system upgrade --install stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  oci://ghcr.io/metio/helm-charts/stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  --create-namespace <span class="se">\
</span></span></span><span class="line"><span class="cl">  --set rbac.clusterAdmin<span class="o">=</span><span class="nb">true</span>
</span></span></code></pre></div><p><code>StageSet</code>s then need nothing tenancy-related — they apply directly:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">stages.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">StageSet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">platform</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">stageset-system</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">platform-app   </span><span class="w"> </span><span class="c"># applied by the controller&#39;s cluster-admin identity</span><span class="w">
</span></span></span></code></pre></div><p>When <code>serviceAccountName</code> is unset and no <code>kubeConfig</code> is given, the controller
applies with its own client — so the <code>cluster-admin</code> binding is what lets those
<code>StageSet</code>s write. The trade-off: every <code>StageSet</code> on the cluster has full write
access, so this is for single-tenant clusters only. Leave <code>rbac.clusterAdmin</code> at its
default <code>false</code> and give each <code>StageSet</code> a <a href="#tenant-serviceaccounts-multi-tenant">tenant ServiceAccount</a>

whenever more than one team shares the cluster. The two mix — a cluster-admin controller
honors <code>serviceAccountName</code> on any <code>StageSet</code> that sets it, dropping to that SA&rsquo;s
rights for that release.</p>
<h2 id="scoping-the-controller-to-a-namespace-set">Scoping the controller to a namespace set</h2>
<p>By default the controller watches every namespace. To run one controller per
tenant-group instead — disjoint deployments that each see only their own
namespaces — set <code>controller.watchNamespaces</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">watchNamespaces</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">team-a</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">team-b</span><span class="w">
</span></span></span></code></pre></div><p>This does two things together:</p>
<ul>
<li><strong>Cache scoping.</strong> The manager&rsquo;s informers only observe <code>StageSet</code>s and sources
in the listed namespaces. Resources elsewhere never enter the cache, so the
controller cannot act on them even if RBAC would allow it.</li>
<li><strong>RBAC pivot.</strong> The chart stops binding the tenant ClusterRole cluster-wide and
instead renders one <code>RoleBinding</code> per listed namespace — defense in depth, so the
apiserver also refuses out-of-scope calls. (The cluster-scoped webhook-caBundle
grant stays a <code>ClusterRoleBinding</code>, since a <code>ValidatingWebhookConfiguration</code> is
not namespaced.)</li>
</ul>
<p>Run several releases with disjoint <code>watchNamespaces</code> lists to shard the cluster
across independent controller instances. Combine it with impersonation for the
tightest setup: each instance sees only its namespaces, and each <code>StageSet</code> is
bounded by its tenant SA.</p>
<h2 id="remote-clusters">Remote clusters</h2>
<p>Point a <code>StageSet</code> at another cluster with <code>kubeConfig</code>, referencing a Secret that
holds a kubeconfig. The controller authenticates to the remote cluster with the
identity in that kubeconfig — token minting is local-cluster only, since a token
minted on the controller&rsquo;s cluster carries the wrong issuer and audience for a
remote apiserver. When <code>serviceAccountName</code> is also set, the controller layers
classic header impersonation onto the kubeconfig identity, so the remote apply runs
as that ServiceAccount on the remote cluster.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">kubeConfig</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod-eu-kubeconfig</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># key defaults to &#34;value&#34; (the Flux convention); set it to override</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app</span><span class="w">
</span></span></span></code></pre></div><p>The Secret is read as the StageSet&rsquo;s <code>spec.serviceAccountName</code>, in the StageSet&rsquo;s
own namespace, so grant that ServiceAccount <code>get</code> on it — a kubeconfig the
ServiceAccount cannot read fails the stage rather than connecting. The kubeconfig
payload defaults to the <code>value</code> key.</p>
<h3 id="cloud-provider-auth">Cloud-provider auth</h3>
<p>Instead of storing a self-contained kubeconfig in a Secret, point <code>kubeConfig</code> at
a <code>configMapRef</code> to authenticate through a cloud provider&rsquo;s workload identity. The
cluster&rsquo;s API server address and CA come from the ConfigMap, and the bearer token
is minted by the provider&rsquo;s IAM/STS on each request:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">kubeConfig</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">configMapRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod-eks-auth</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nn">---</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ConfigMap</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod-eks-auth</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">data</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">provider: aws              # one of</span><span class="p">:</span><span class="w"> </span><span class="l">aws, azure, gcp, generic (required)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">cluster</span><span class="p">:</span><span class="w"> </span><span class="l">prod-eks         </span><span class="w"> </span><span class="c"># provider-specific cluster resource name</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">address</span><span class="p">:</span><span class="w"> </span><span class="l">https://XXXX.eks.amazonaws.com </span><span class="w"> </span><span class="c"># API server address</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">ca.crt</span><span class="p">:</span><span class="w"> </span><span class="l">|                 </span><span class="w"> </span><span class="c"># PEM-encoded CA bundle (optional)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>-----<span class="l">BEGIN CERTIFICATE-----</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="l">...</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>-----<span class="l">END CERTIFICATE-----</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">audiences</span><span class="p">:</span><span class="w"> </span><span class="l">|              </span><span class="w"> </span><span class="c"># OIDC audiences for the SA token (optional)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="l">sts.amazonaws.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">deployer </span><span class="w"> </span><span class="c"># cloud identity to impersonate (optional)</span><span class="w">
</span></span></span></code></pre></div><p>The ConfigMap is read as the StageSet&rsquo;s <code>spec.serviceAccountName</code>, like the
<code>secretRef</code> payload above, so that ServiceAccount needs <code>get</code> on it. Which keys
are required depends on the provider; the <code>provider</code> key is always required and
must be one of
<code>aws</code>, <code>azure</code>, <code>gcp</code>, or <code>generic</code>. When <code>serviceAccountName</code> is set in the
ConfigMap, that ServiceAccount (in the StageSet&rsquo;s namespace) is the cloud identity
whose workload-identity binding the provider exchanges for a cluster token; when
absent, the controller&rsquo;s own ambient cloud credentials are used.</p>
<p><strong>Trust model:</strong> the ConfigMap names the provider and the cluster, but the actual
cloud credential is never in the cluster — it is minted on demand from the cloud&rsquo;s
IAM/STS using the controller&rsquo;s (or the named ServiceAccount&rsquo;s) workload identity.
A malformed ConfigMap (unknown <code>provider</code>, missing a required key, or a missing
ConfigMap) fails the StageSet terminally with <code>reason: InvalidSpec</code>; admission
only checks that <code>configMapRef.name</code> is set, because the ConfigMap contents are
not visible to the webhook.</p>
<p><code>secretRef</code> and <code>configMapRef</code> are mutually exclusive — set exactly one.</p>
<p>Cross-namespace <code>sourceRef</code> and <code>dependsOn</code> references can be disabled
cluster-wide with the controller&rsquo;s <code>--no-cross-namespace-refs</code> flag when you want
hard namespace isolation.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/multi-cluster" term="multi-cluster" label="multi-cluster"/><category scheme="https://stageset.projects.metio.wtf/tags/tenancy" term="tenancy" label="tenancy"/><category scheme="https://stageset.projects.metio.wtf/tags/impersonation" term="impersonation" label="impersonation"/><category scheme="https://stageset.projects.metio.wtf/tags/rbac" term="rbac" label="rbac"/></entry><entry><title type="html">Network policy</title><link href="https://stageset.projects.metio.wtf/security/network-policy/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/security/service-mesh/?utm_source=atom_feed" rel="related" type="text/html" title="Service mesh"/><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://stageset.projects.metio.wtf/security/image-verification/?utm_source=atom_feed" rel="related" type="text/html" title="Image verification"/><link href="https://stageset.projects.metio.wtf/runbooks/imageunverified/?utm_source=atom_feed" rel="related" type="text/html" title="ImageUnverified"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><id>https://stageset.projects.metio.wtf/security/network-policy/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The opt-in NetworkPolicy the chart ships — pod-scoped allowlists vs. a namespace-wide default-deny, choosing a policy engine, the ingress and egress traffic the controller needs, and how to tighten each port.</blockquote><p>The Helm chart ships an opt-in <code>NetworkPolicy</code> for the controller pod. It is off by
default and renders only when <code>networkPolicy.enabled</code> is <code>true</code>. Two independent
layers are on offer: pod-scoped allowlists that lock down only the controller&rsquo;s own
pods (the safe default), and an additional namespace-wide default-deny for a
zero-trust namespace. The ingress and egress tables below describe exactly what
traffic the controller depends on, so everything else can be denied.</p>
<h2 id="two-layers-pod-scoped-allowlists-vs-namespace-default-deny">Two layers: pod-scoped allowlists vs. namespace default-deny</h2>
<p><code>networkPolicy.enabled: true</code> renders per-workload, <strong>pod-scoped allowlist</strong>
policies. They select only the controller&rsquo;s own pods through their selector labels
and lock down just those pods to the required ports. This is the safe default and is
fine in a shared namespace: co-located workloads — including anything in
<code>flux-system</code> if the controller shares that namespace — are untouched.</p>
<p><code>networkPolicy.defaultDeny.enabled</code> (default <code>false</code>) <strong>additionally</strong> renders a
namespace-wide default-deny so every pod in the namespace is denied by default and
the allowlists become the only exceptions (a zero-trust namespace). The default-deny
sits at a lower precedence than the allowlists, so the allowlists always win for the
controller pods while everything else is denied.</p>
<p>Pick the layer that matches namespace ownership:</p>
<ul>
<li><strong><code>defaultDeny.enabled: false</code></strong> (default) — pod-scoped setup. Only the
controller&rsquo;s pods are locked down; neighbours keep whatever posture their own
policies give them.</li>
<li><strong><code>defaultDeny.enabled: true</code></strong> — namespace zero-trust. Enable this <strong>only when
the controller owns its namespace</strong>, because the deny-all also denies every
co-located workload that does not have its own allowing policy.</li>
</ul>
<p><code>defaultDeny.order</code> (default <code>2000</code>) tunes the Calico <code>order</code> / ClusterNetworkPolicy
<code>priority</code> that keeps the deny-all subordinate to the allowlists. The <code>kubernetes</code>
and <code>cilium</code> engines have no precedence knob — deny and allow combine additively and
allow wins — so the value matters only for the <code>calico</code> and <code>clusterNetworkPolicy</code>
engines.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">defaultDeny</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">   </span><span class="c"># only when the controller owns this namespace</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">order</span><span class="p">:</span><span class="w"> </span><span class="m">2000</span><span class="w">
</span></span></span></code></pre></div><h2 id="choosing-a-policy-engine">Choosing a policy engine</h2>
<p><code>networkPolicy.engine</code> selects which policy dialect the chart renders. It is
explicit, not auto-detected: a chart that sniffed the running CNI would render
different objects on different clusters from identical values, which breaks GitOps
determinism. You name the engine, and the rendered manifest is the same everywhere.</p>
<table>
	<thead>
			<tr>
					<th><code>engine</code></th>
					<th>Renders</th>
					<th>API</th>
					<th>FQDN egress</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>kubernetes</code> (default)</td>
					<td><code>NetworkPolicy</code></td>
					<td><code>networking.k8s.io/v1</code></td>
					<td>No</td>
			</tr>
			<tr>
					<td><code>cilium</code></td>
					<td><code>CiliumNetworkPolicy</code></td>
					<td><code>cilium.io/v2</code></td>
					<td>Yes — free <code>toFQDNs</code> egress</td>
			</tr>
			<tr>
					<td><code>calico</code></td>
					<td><code>NetworkPolicy</code></td>
					<td><code>projectcalico.org/v3</code></td>
					<td>No — OSS Calico has no FQDN egress; that is Calico Enterprise only</td>
			</tr>
			<tr>
					<td><code>clusterNetworkPolicy</code></td>
					<td><code>ClusterNetworkPolicy</code></td>
					<td><code>policy.networking.k8s.io/v1alpha2</code></td>
					<td>No</td>
			</tr>
	</tbody>
</table>
<p><code>clusterNetworkPolicy</code> renders the SIG-Network <code>ClusterNetworkPolicy</code> that
consolidates the deprecated <code>AdminNetworkPolicy</code> + <code>BaselineAdminNetworkPolicy</code>
APIs into one resource. It is alpha, cluster-scoped, and rendered in the <code>Baseline</code>
tier so a developer-authored <code>NetworkPolicy</code> still takes precedence over it.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">cilium</span><span class="w">
</span></span></span></code></pre></div><p>The per-port <code>.from</code> knobs documented under <a href="#configuring-ingress">Configuring ingress</a>

apply to the <code>kubernetes</code> engine only. For the other engines the allowlists are
pod-scoped allow-all on the required ports, and you tighten them through that
engine&rsquo;s native passthrough lists — <code>networkPolicy.&lt;engine&gt;.ingress</code> and
<code>networkPolicy.&lt;engine&gt;.egress</code> — which are merged verbatim into the rendered
policy&rsquo;s <code>spec</code>. For example, adding identity-based ingress and a <code>toFQDNs</code> egress
under the Cilium engine:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">cilium</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">cilium</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">ingress</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">fromEndpoints</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">matchLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">app.kubernetes.io/name</span><span class="p">:</span><span class="w"> </span><span class="l">flagger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">egress</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">toFQDNs</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">matchName</span><span class="p">:</span><span class="w"> </span><span class="l">bucket.example.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">toPorts</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">ports</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span>- <span class="nt">port</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;443&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="nt">protocol</span><span class="p">:</span><span class="w"> </span><span class="l">TCP</span><span class="w">
</span></span></span></code></pre></div><h2 id="required-traffic">Required traffic</h2>
<p>The two tables below are the engine-agnostic matrix of what the controller pod must
be allowed to send and receive. Some peers are selectable by pod or namespace label;
others — the kube-apiserver and out-of-cluster endpoints — have no label to select
on and must be expressed as <code>ipBlock</code> CIDRs.</p>
<h3 id="ingress">Ingress</h3>
<table>
	<thead>
			<tr>
					<th>Port</th>
					<th>Source</th>
					<th>Mode</th>
					<th>Selectable by label?</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Webhook (<code>ports.webhook</code>, <code>9443</code>)</td>
					<td>The kube-apiserver, dialing the validating admission webhook</td>
					<td>webhook</td>
					<td>No — the apiserver is not a pod</td>
			</tr>
			<tr>
					<td>Metrics (<code>ports.metrics</code>, <code>8080</code>)</td>
					<td>Prometheus scraping <code>/metrics</code></td>
					<td>always</td>
					<td>Yes — by the scraper&rsquo;s pod or namespace</td>
			</tr>
			<tr>
					<td>Gate (<code>ports.gate</code>, <code>8082</code>)</td>
					<td>Flagger, polling the read-only gate endpoint for the promotion verdict</td>
					<td>gate</td>
					<td>Yes — by Flagger&rsquo;s namespace</td>
			</tr>
	</tbody>
</table>
<p>The metrics rule renders unconditionally, so the policy is never an empty-ingress
deny-all by accident. The webhook rule renders only when <code>webhook.enabled</code>, and the
gate rule only when <code>gate.enabled</code>.</p>
<p>The apiserver sources traffic from an address that is not a pod, so the webhook rule
cannot be narrowed with a <code>podSelector</code> or <code>namespaceSelector</code>. Leaving the webhook
<code>from</code> list empty keeps the port reachable, which is what lets the apiserver reach
the webhook. Authenticity on the webhook port is enforced by TLS and the CA bundle
on the <code>ValidatingWebhookConfiguration</code>, not by the network layer — see the
<a href="/security/admission-webhook/">admission webhook page</a>
.</p>
<h3 id="egress">Egress</h3>
<p>Egress only matters when you opt into it (<code>networkPolicy.egress.enabled</code>). The
controller needs the following outbound flows.</p>
<table>
	<thead>
			<tr>
					<th>Destination</th>
					<th>Purpose</th>
					<th>Selectable by label?</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Cluster DNS</td>
					<td>Name resolution — without it every other egress flow fails</td>
					<td>Yes — by the DNS namespace</td>
			</tr>
			<tr>
					<td>Local kube-apiserver</td>
					<td>All controller reads and writes go through the apiserver</td>
					<td>No — <code>ipBlock</code> CIDR only</td>
			</tr>
			<tr>
					<td>Remote-cluster apiserver</td>
					<td>A StageSet&rsquo;s <code>spec.kubeConfig</code> points the apply at another cluster&rsquo;s apiserver</td>
					<td>No — <code>ipBlock</code> CIDR only</td>
			</tr>
			<tr>
					<td>source-controller / producer</td>
					<td>Artifact fetch from Flux&rsquo;s source-controller or a producer&rsquo;s namespace</td>
					<td>Yes — <code>flux-system</code> / the producer namespace</td>
			</tr>
			<tr>
					<td>S3 rollback store</td>
					<td>The S3-compatible bucket backing the rollback store, when configured</td>
					<td>Depends — in-cluster MinIO is label-selectable; an external bucket is <code>ipBlock</code> only</td>
			</tr>
			<tr>
					<td>OTLP collector</td>
					<td>Shipping traces when an OTLP endpoint is configured</td>
					<td>Depends — in-cluster collector is label-selectable; an external one is <code>ipBlock</code> only</td>
			</tr>
			<tr>
					<td><code>http</code> action / direct <code>sourceRef</code> URLs</td>
					<td>Targets reached by <code>http</code> actions and any direct <code>sourceRef</code> URL</td>
					<td>No — <code>ipBlock</code> CIDR only</td>
			</tr>
	</tbody>
</table>
<p>The kube-apiserver is never label-selectable, so its egress rule must be an
<code>ipBlock</code> CIDR. The same applies to any remote-cluster apiserver, S3 bucket, OTLP
collector, or <code>http</code>-action target that lives outside the cluster. See
<a href="/security/multi-cluster/">multi-cluster</a>
, <a href="/gating/rollback/">rollback</a>
,
<a href="/integrations/producer-aware-sources/">producer-aware sources</a>
, and
<a href="/defining-a-release/actions/">actions</a>
 for the flows behind each row.</p>
<h2 id="configuring-ingress">Configuring ingress</h2>
<p>Under the <code>kubernetes</code> engine, enable the policy and tighten each port through its
<code>from</code> knob. An empty <code>from</code> list leaves that port open; a non-empty list restricts
it to the listed peers.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="c"># The apiserver dials the webhook and has no label to select on — leave open.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">from</span><span class="p">:</span><span class="w"> </span><span class="p">[]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="c"># Scope metrics scraping to the monitoring namespace.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">namespaceSelector</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">matchLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">kubernetes.io/metadata.name</span><span class="p">:</span><span class="w"> </span><span class="l">monitoring</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="c"># Scope the gate to Flagger&#39;s namespace.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">gate</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">namespaceSelector</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">matchLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">kubernetes.io/metadata.name</span><span class="p">:</span><span class="w"> </span><span class="l">flagger-system</span><span class="w">
</span></span></span></code></pre></div><p>Anything the per-port knobs do not cover goes into <code>additionalIngress</code>, which is
merged verbatim into the policy:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">additionalIngress</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">ports</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span>- <span class="nt">protocol</span><span class="p">:</span><span class="w"> </span><span class="l">TCP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">port</span><span class="p">:</span><span class="w"> </span><span class="m">8080</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span>- <span class="nt">namespaceSelector</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">matchLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">kubernetes.io/metadata.name</span><span class="p">:</span><span class="w"> </span><span class="l">monitoring</span><span class="w">
</span></span></span></code></pre></div><h2 id="opt-in-egress">Opt-in egress</h2>
<p>Egress is off by default, and deliberately so. Adding the <code>Egress</code> policy type flips
the controller pod to default-deny for outbound traffic — everything not explicitly
allowed is dropped. Getting the allow-list complete is the cluster operator&rsquo;s risk,
because most of the controller&rsquo;s outbound peers — the kube-apiserver, any
remote-cluster apiserver, S3, the OTLP collector, <code>http</code>-action targets — have no
label to select on and so depend on <code>ipBlock</code> CIDRs that vary per cluster. An
incomplete list does not fail loudly; it silently cuts that path off.</p>
<blockquote>
<p><strong>Warning:</strong> Enabling egress <strong>without</strong> an <code>ipBlock</code> for the kube-apiserver cuts
the controller off from the cluster — it can no longer read or write any object,
and every reconcile fails. Always include the apiserver CIDR before turning egress
on.</p>
</blockquote>
<p>Find the apiserver&rsquo;s address with:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace default get endpointslice kubernetes --output <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">&#39;{.endpoints[*].addresses[*]}&#39;</span>
</span></span></code></pre></div><p>Use that IP as a <code>/32</code> (or your control plane&rsquo;s CIDR for an HA apiserver). A
complete controller egress block — DNS, the apiserver, source-controller, S3, and an
OTLP collector — looks like this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">egress</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># DNS to the cluster DNS namespace. Without this, every flow below</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># fails name resolution.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">dns</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">dnsNamespace</span><span class="p">:</span><span class="w"> </span><span class="l">kube-system</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># kube-apiserver — not label-selectable, so an ipBlock CIDR.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># Replace with the IP(s) from the command above.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">ipBlock</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">cidr</span><span class="p">:</span><span class="w"> </span><span class="m">10.0.0.1</span><span class="l">/32</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">ports</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">protocol</span><span class="p">:</span><span class="w"> </span><span class="l">TCP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">port</span><span class="p">:</span><span class="w"> </span><span class="m">443</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># source-controller — artifact fetch from the flux-system namespace.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">namespaceSelector</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">matchLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="nt">kubernetes.io/metadata.name</span><span class="p">:</span><span class="w"> </span><span class="l">flux-system</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># S3 rollback store — an ipBlock CIDR for an external endpoint. For</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># in-cluster MinIO, use a namespaceSelector instead.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">ipBlock</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">cidr</span><span class="p">:</span><span class="w"> </span><span class="m">198.51.100.0</span><span class="l">/24</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">ports</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">protocol</span><span class="p">:</span><span class="w"> </span><span class="l">TCP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">port</span><span class="p">:</span><span class="w"> </span><span class="m">443</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># OTLP collector — an ipBlock CIDR for an external endpoint. For an</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="c"># in-cluster collector, use a namespaceSelector instead.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">ipBlock</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">cidr</span><span class="p">:</span><span class="w"> </span><span class="m">203.0.113.10</span><span class="l">/32</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">ports</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">protocol</span><span class="p">:</span><span class="w"> </span><span class="l">TCP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">port</span><span class="p">:</span><span class="w"> </span><span class="m">4317</span><span class="w">
</span></span></span></code></pre></div><p>Add an <code>ipBlock</code> peer for each remote-cluster apiserver named by a <code>spec.kubeConfig</code>
and for each <code>http</code>-action / direct <code>sourceRef</code> host the controller reaches. Trim
the rest to what your install actually uses: drop the S3 block when no rollback store
is configured, and drop the OTLP block when tracing is off. The apiserver and DNS
rules are non-negotiable for the controller.</p>
<p>For the full set of chart values, see
<a href="/reference/helm-values/">Helm chart values</a>
.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/network-policy" term="network-policy" label="network-policy"/><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/><category scheme="https://stageset.projects.metio.wtf/tags/networking" term="networking" label="networking"/></entry><entry><title type="html">Secrets encryption</title><link href="https://stageset.projects.metio.wtf/security/encryption/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://stageset.projects.metio.wtf/running/disaster-recovery/?utm_source=atom_feed" rel="related" type="text/html" title="Backup and disaster recovery"/><link href="https://stageset.projects.metio.wtf/security/image-verification/?utm_source=atom_feed" rel="related" type="text/html" title="Image verification"/><link href="https://stageset.projects.metio.wtf/runbooks/imageunverified/?utm_source=atom_feed" rel="related" type="text/html" title="ImageUnverified"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><id>https://stageset.projects.metio.wtf/security/encryption/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Decrypt SOPS-encrypted files in a stage&rsquo;s source before they are applied.</blockquote><p>A stage&rsquo;s source can carry <a href="https://github.com/getsops/sops">SOPS</a>
-encrypted
files — typically a <code>Secret</code> whose values are encrypted — and the controller
decrypts them in memory, before building and applying the manifests. This mirrors
Flux&rsquo;s <code>kustomize-controller</code> decryption contract, so an existing SOPS-encrypted
repository needs no migration.</p>
<p>Set <code>spec.decryption</code> and point it at a Secret holding the keys:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">stages.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">StageSet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">decryption</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">provider</span><span class="p">:</span><span class="w"> </span><span class="l">sops         </span><span class="w"> </span><span class="c"># the only provider</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">sops-age       </span><span class="w"> </span><span class="c"># a Secret in this namespace holding the age key</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">GitRepository</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-config  </span><span class="w"> </span><span class="c"># contains an encrypted secret.yaml</span><span class="w">
</span></span></span></code></pre></div><h2 id="walkthrough--age">Walkthrough — age</h2>
<p><a href="https://age-encryption.org/">age</a>
 is the simplest key type and needs no external
service. Take a <code>Secret</code> from plaintext to a GitOps-safe rollout in four steps.</p>
<p><strong>1. Generate an age key.</strong> The file holds the private key; the printed <code>age1…</code>
line is the public recipient to encrypt to.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">age-keygen -o age.agekey
</span></span><span class="line"><span class="cl"><span class="c1"># public key: age1qz…</span>
</span></span></code></pre></div><p><strong>2. Encrypt a Secret.</strong> Encrypt only its values, so the file stays a valid
Kubernetes object, then commit <code>secret.enc.yaml</code> (never the plaintext):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># secret.yaml</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">Secret</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-db</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">stringData</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">password</span><span class="p">:</span><span class="w"> </span><span class="l">s3cr3t-do-not-commit-plaintext</span><span class="w">
</span></span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">sops --encrypt --age age1qz… <span class="se">\
</span></span></span><span class="line"><span class="cl">  --encrypted-regex <span class="s1">&#39;^(data|stringData)$&#39;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  secret.yaml &gt; secret.enc.yaml
</span></span></code></pre></div><p><strong>3. Put the private key in the cluster</strong> under a <code>.agekey</code> data entry. Store
<code>age.agekey</code> itself somewhere safe — it is the only thing that can decrypt the
Secret.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">kubectl create secret generic sops-age <span class="se">\
</span></span></span><span class="line"><span class="cl">  --namespace payments <span class="se">\
</span></span></span><span class="line"><span class="cl">  --from-file<span class="o">=</span>keys.agekey<span class="o">=</span>age.agekey
</span></span></code></pre></div><p><strong>4. Decrypt on rollout.</strong> Point a <code>StageSet</code> at the source holding
<code>secret.enc.yaml</code> and set <code>spec.decryption</code> (as in the example above). On reconcile
the controller fetches the source, decrypts every SOPS file in memory, builds, and
applies — so the cluster holds the plaintext <code>payments-db</code> Secret while Git only
ever held ciphertext. Grant the deployer ServiceAccount read access to the key
Secret (see <a href="#how-keys-are-read--tenancy">tenancy</a>
 below).</p>
<h2 id="pairing-with-jaas-rendered-manifests">Pairing with JaaS-rendered manifests</h2>
<p>A realistic app renders its config from Jsonnet with
<a href="https://jaas.projects.metio.wtf/">JaaS</a>
 and keeps only its Secret encrypted. The
two compose cleanly because each owns one concern:</p>
<ul>
<li><strong>JaaS renders the non-secret manifests.</strong> It evaluates Jsonnet server-side and
cannot hold secret values: SOPS ciphertext carries a MAC over the whole encrypted
document, so it can&rsquo;t be authored in Jsonnet — and routing plaintext secrets
through a render service is what you are avoiding.</li>
<li><strong>The Secret stays SOPS-encrypted in Git</strong>, as in the walkthrough.</li>
<li><strong>The controller decrypts and orders both</strong> under one <code>spec.decryption</code>:</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">decryption</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">provider</span><span class="p">:</span><span class="w"> </span><span class="l">sops</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">sops-age</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">secrets                </span><span class="w"> </span><span class="c"># decrypt + apply the SOPS Secret first</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">GitRepository</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-secrets</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">app                    </span><span class="w"> </span><span class="c"># then the JaaS-rendered app that mounts it</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">jaas.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">JsonnetSnippet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-app</span><span class="w">
</span></span></span></code></pre></div><p>The <code>secrets</code> stage runs first; only once the <code>Secret</code> is applied does the <code>app</code>
stage roll out the rendered manifests that mount it. The encrypted Secret and the
rendered config live in separate sources, so the Jsonnet author never touches secret
material.</p>
<h2 id="the-fields">The fields</h2>
<ul>
<li><strong><code>provider</code></strong> — the decryption backend. Only <code>sops</code> is supported.</li>
<li><strong><code>secretRef.name</code></strong> — a Secret in the <code>StageSet</code>&rsquo;s namespace holding the keys,
using the SOPS conventions: age private keys under data entries ending in
<code>.agekey</code>, armored PGP private keys under <code>.asc</code>. Optional — omit it for a
<a href="#cloud-kms">cloud-KMS-only</a>
 setup.</li>
</ul>
<h2 id="how-keys-are-read--tenancy">How keys are read — tenancy</h2>
<p>The key Secret is read in the <code>StageSet</code>&rsquo;s namespace <strong>under its
<code>serviceAccountName</code></strong>, exactly like the manifests it applies. A tenant can only
decrypt with key material its own ServiceAccount is allowed to read, so a key in one
namespace is never reachable from another. Grant the deployer SA <code>get</code> on the key
Secret:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">rbac.authorization.k8s.io/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">Role</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-deployer-sops</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rules</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">apiGroups</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;&#34;</span><span class="p">]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">resources</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">secrets]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">resourceNames</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">sops-age]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">verbs</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">get]</span><span class="w">
</span></span></span></code></pre></div><p>In a <a href="/security/multi-cluster/#single-tenant-cluster-admin">single-tenant cluster-admin</a>

install (no <code>serviceAccountName</code>), the controller reads the key Secret under its
own identity instead.</p>
<h2 id="decryption-and-the-rollback-store">Decryption and the rollback store</h2>
<p>Decrypted bytes exist only in memory on the apply path. The one place rendered
output is persisted is the optional <a href="/gating/rollback/">rollback store</a>
, which is
<strong>encrypted at rest</strong> (S3 SSE by default; an encrypted volume for the file store) —
so a decrypted <code>Secret</code> never lands in plaintext on disk. See
<a href="/gating/rollback/#encryption-at-rest">encryption at rest</a>
.</p>
<p>A rollback re-fetches the previous source and <strong>runs decryption again</strong> rather than
restoring plaintext, so the key Secret must still exist when a rollback fires. If
the key was rotated or deleted in the meantime, the rollback <strong>fails closed</strong> with
<code>PreviousRevisionUnavailable</code> instead of applying a stale or unreadable Secret — an
encrypted store cannot avoid this, and it is the safe failure direction.</p>
<h2 id="cloud-kms">Cloud KMS</h2>
<p>SOPS files encrypted with a cloud KMS key (AWS KMS, GCP KMS, Azure Key Vault, or
HashiCorp Vault) decrypt through the <strong>controller&rsquo;s ambient credentials</strong> — e.g. an
IRSA role on EKS, wired via <code>serviceAccount.annotations</code>. No in-cluster key Secret
is needed, so <code>secretRef</code> may be omitted for a KMS-only <code>StageSet</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">decryption</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">provider</span><span class="p">:</span><span class="w"> </span><span class="l">sops         </span><span class="w"> </span><span class="c"># secretRef omitted; KMS uses the controller&#39;s identity</span><span class="w">
</span></span></span></code></pre></div><p>One consequence to weigh in a multi-tenant cluster: unlike age (read under the
tenant SA), <strong>cloud KMS uses the controller&rsquo;s identity</strong>, so any <code>StageSet</code> can
decrypt a file encrypted with a KMS key the controller&rsquo;s role can access. This
matches Flux&rsquo;s <code>kustomize-controller</code>. Scope the controller&rsquo;s KMS grant
accordingly, use age keys for hard per-tenant isolation, or enable
<a href="#per-tenant-kms-identity-object-level-kms">object-level KMS identity</a>
 below.</p>
<h3 id="per-tenant-kms-identity-object-level-kms">Per-tenant KMS identity (object-level KMS)</h3>
<p>By default cloud KMS uses the controller&rsquo;s ambient credentials (above). Enabling
the controller flag <code>--object-level-kms</code> instead decrypts each cloud KMS key with
the <strong><code>StageSet</code>&rsquo;s <code>serviceAccountName</code> federated to a cloud identity</strong>, so KMS
decryption is bounded by that tenant&rsquo;s cloud IAM grants rather than the
controller&rsquo;s. The flag is off by default; leaving it off keeps the ambient
behavior unchanged.</p>
<p>When the flag is on and a <code>StageSet</code> sets <code>serviceAccountName</code>, the controller
mints a projected token for that ServiceAccount and exchanges it with the cloud&rsquo;s
STS — AWS IRSA, GCP Workload Identity, or Azure Workload Identity — exactly like
the per-tenant cluster credentials used elsewhere. A <code>StageSet</code> with no
<code>serviceAccountName</code> falls back to the ambient credentials (there is no tenant
identity to assume). age and PGP decryption are unaffected by the flag — they
always run tenant-scoped from <code>secretRef</code>.</p>
<p>Trust model: the KMS call carries the tenant ServiceAccount&rsquo;s cloud identity, so
one tenant can only decrypt with KMS keys its own cloud identity is granted —
matching the isolation age already gives. The tenant ServiceAccount must be
federated to a cloud identity (e.g. an IRSA-annotated ServiceAccount, a GKE
Workload-Identity binding, or an Azure federated credential) that holds
<code>kms:Decrypt</code> on the relevant key; without that federation the KMS decrypt fails.
There is no spec field — the choice is a cluster-wide operator decision via the
flag.</p>
<h2 id="whats-supported">What&rsquo;s supported</h2>
<ul>
<li><strong>age</strong> keys via <code>secretRef</code> — read under the tenant SA. The resource-level
pattern (<code>--encrypted-regex '^(data|stringData)$'</code>) is the tested path.</li>
<li><strong>PGP</strong> keys via <code>secretRef</code> (<code>.asc</code> entries) — read under the tenant SA, pure
Go, no <code>gpg</code> binary or keyring needed. See <a href="#pgp-keys">PGP keys</a>
.</li>
<li><strong>Cloud KMS</strong> (AWS/GCP/Azure/Vault) via the controller&rsquo;s ambient credentials,
or via each tenant&rsquo;s federated cloud identity with
<a href="#per-tenant-kms-identity-object-level-kms"><code>--object-level-kms</code></a>
.</li>
<li><strong>Encrypted files feeding a <code>secretGenerator</code></strong> — an encrypted <code>.env</code> (or other
file) referenced by a kustomize <code>secretGenerator</code> is decrypted before the build,
so the generated <code>Secret</code> carries the plaintext.</li>
<li>A file with no SOPS metadata passes through untouched, so encrypted and plain
manifests can sit side by side in one source.</li>
</ul>
<h2 id="pgp-keys">PGP keys</h2>
<p>PGP works <strong>tenant-scoped</strong>, like age: put one or more armored private keys in the
<code>secretRef</code> Secret under data entries suffixed <code>.asc</code>. The data key is decrypted in
pure Go (<code>ProtonMail/go-crypto</code>) directly from those keys — <strong>no <code>gpg</code> binary, no
GnuPG keyring, and no <code>GNUPGHOME</code></strong> — and the keys are read under the <code>StageSet</code>&rsquo;s
<code>serviceAccountName</code>, so a tenant can only use material its ServiceAccount can read.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># export the armored private key and load it into the key Secret</span>
</span></span><span class="line"><span class="cl">gpg --export-secret-keys --armor 0xYOURFINGERPRINT &gt; key.asc
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">kubectl create secret generic sops-keys <span class="se">\
</span></span></span><span class="line"><span class="cl">  --namespace payments <span class="se">\
</span></span></span><span class="line"><span class="cl">  --from-file<span class="o">=</span>pgp.asc<span class="o">=</span>key.asc
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">decryption</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">provider</span><span class="p">:</span><span class="w"> </span><span class="l">sops</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">sops-keys     </span><span class="w"> </span><span class="c"># holds the *.asc private key(s)</span><span class="w">
</span></span></span></code></pre></div><p>One Secret can carry both age (<code>*.agekey</code>) and PGP (<code>*.asc</code>) keys; the right one is
used per file. For a fresh setup, age is simpler and the recommended default, but an
existing PGP-encrypted repository needs no migration.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/secrets" term="secrets" label="secrets"/><category scheme="https://stageset.projects.metio.wtf/tags/encryption" term="encryption" label="encryption"/><category scheme="https://stageset.projects.metio.wtf/tags/sops" term="sops" label="sops"/><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/></entry><entry><title type="html">Service mesh</title><link href="https://stageset.projects.metio.wtf/security/service-mesh/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/security/network-policy/?utm_source=atom_feed" rel="related" type="text/html" title="Network policy"/><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://stageset.projects.metio.wtf/security/image-verification/?utm_source=atom_feed" rel="related" type="text/html" title="Image verification"/><link href="https://stageset.projects.metio.wtf/runbooks/imageunverified/?utm_source=atom_feed" rel="related" type="text/html" title="ImageUnverified"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><id>https://stageset.projects.metio.wtf/security/service-mesh/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The opt-in service-mesh authorization the chart ships — Istio or Linkerd identity-based authorization and mTLS layered over networkPolicy, per-port allowed mesh identities, the non-mesh carve-outs for the apiserver and kubelet, and native passthrough.</blockquote><p>The Helm chart ships an opt-in service-mesh authorization layer for the controller
pod. It is off by default and renders only when <code>serviceMesh.enabled</code> is <code>true</code>.
Where the <a href="/security/network-policy/">network policy</a>
 operates at L3/L4 — which pods and
IP ranges may reach which ports — the service mesh operates at L7 with
<strong>identity-based authorization</strong> and <strong>mTLS</strong>: it asks <em>which mesh identity</em> is
calling, proven by a cryptographic workload certificate, not merely which IP the
packet came from.</p>
<p><code>serviceMesh</code> and <code>networkPolicy</code> are separate fields and compose additively. They
solve different problems and are best enabled together: the network policy draws the
L3/L4 perimeter, and the mesh authorizes meshed callers by SPIFFE identity on top of
it. Enabling one does not require the other, and neither weakens the other.</p>
<h2 id="opt-in-and-explicit-engine">Opt-in and explicit engine</h2>
<p><code>serviceMesh.engine</code> selects which mesh dialect the chart renders. It is explicit,
not auto-detected: a chart that sniffed the running mesh would render different
objects on different clusters from identical values, which breaks GitOps determinism.
You name the engine, and the rendered manifest is the same everywhere.</p>
<table>
	<thead>
			<tr>
					<th><code>engine</code></th>
					<th>Renders</th>
					<th>API</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>istio</code> (default)</td>
					<td><code>AuthorizationPolicy</code> + (optional) <code>PeerAuthentication</code></td>
					<td><code>security.istio.io/v1</code></td>
			</tr>
			<tr>
					<td><code>linkerd</code></td>
					<td><code>Server</code> + <code>AuthorizationPolicy</code> + <code>MeshTLSAuthentication</code></td>
					<td><code>policy.linkerd.io</code></td>
			</tr>
	</tbody>
</table>
<p>The rendered objects are inert unless the named mesh is actually installed and the
controller pod is injected into it. Enabling <code>serviceMesh</code> on an un-meshed pod renders
the manifests but changes nothing about how traffic flows.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">istio</span><span class="w">
</span></span></span></code></pre></div><h2 id="per-port-authorization">Per-port authorization</h2>
<p>Each mesh-reachable port carries a <code>from</code> list naming the mesh identities allowed to
call it. An <strong>empty <code>from</code> list leaves that port open</strong> to any meshed caller,
mirroring <code>networkPolicy</code>&rsquo;s empty-<code>from</code>-is-open semantics; a non-empty list
restricts the port to the listed identities.</p>
<p>The mesh-reachable ports are the gate port and the metrics port:</p>
<table>
	<thead>
			<tr>
					<th>Port</th>
					<th>Mode</th>
					<th><code>from</code> restricts</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Gate (<code>ports.gate</code>, <code>8082</code>)</td>
					<td>gate</td>
					<td>Flagger polling the read-only gate endpoint</td>
			</tr>
			<tr>
					<td>Metrics (<code>ports.metrics</code>, <code>8080</code>)</td>
					<td>always</td>
					<td>Prometheus scraping <code>/metrics</code></td>
			</tr>
	</tbody>
</table>
<p>A <code>from</code> entry is a source matcher with two fields:</p>
<ul>
<li><strong><code>principals</code></strong> — SPIFFE/mesh identities. On Istio these are
<code>source.principals</code> (<code>cluster.local/ns/&lt;ns&gt;/sa/&lt;sa&gt;</code>). On Linkerd they map to
<code>MeshTLSAuthentication</code> identities (<code>&lt;sa&gt;.&lt;ns&gt;.serviceaccount.identity.linkerd.cluster.local</code>,
or <code>*</code>).</li>
<li><strong><code>namespaces</code></strong> — source namespaces (Istio <code>source.namespaces</code>). <strong>Istio-only</strong> —
Linkerd authenticates by workload identity, not by namespace, so this field is
ignored under the <code>linkerd</code> engine.</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">istio</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="c"># Restrict the gate to Flagger&#39;s identity.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">gate</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">principals</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="l">cluster.local/ns/flagger-system/sa/flagger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="c"># Scope metrics scraping to the monitoring namespace.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">namespaces</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="l">monitoring</span><span class="w">
</span></span></span></code></pre></div><h2 id="non-mesh-clients-keep-working">Non-mesh clients keep working</h2>
<p>The kube-apiserver (which dials the admission webhook) and the kubelet (which dials
the readiness and liveness probes) are <strong>not part of the mesh</strong>. They carry no mesh
identity and present no workload certificate, so any authorization rule that demanded
one would reject them — admission would break and probes would fail.</p>
<p>The chart deliberately leaves the <strong>webhook and health probe ports open</strong> so these
non-mesh clients always connect:</p>
<ul>
<li><strong>Istio</strong> adds an allow-any rule covering the webhook (<code>9443</code>) and health (<code>8081</code>)
ports, so no identity is required on them.</li>
<li>With <strong><code>mtls: strict</code></strong>, the chart additionally sets <code>portLevelMtls: PERMISSIVE</code> on
those two ports, so a plaintext connection from the apiserver or kubelet is still
accepted even while every other port enforces mTLS.</li>
<li><strong>Linkerd</strong> renders no <code>Server</code> for those ports, leaving them outside the mesh&rsquo;s
authorization scope entirely.</li>
</ul>
<p>This is why admission and probes keep working with the mesh fully enabled.
Authenticity on the webhook port is enforced by TLS and the CA bundle on the
<code>ValidatingWebhookConfiguration</code>, not by the mesh — see the
<a href="/security/admission-webhook/">admission webhook page</a>
.</p>
<h2 id="mtls">mTLS</h2>
<p><code>serviceMesh.mtls</code> sets the mTLS posture and applies to the <strong>Istio engine only</strong>;
Linkerd negotiates mTLS automatically between meshed pods, so the knob is ignored
there.</p>
<table>
	<thead>
			<tr>
					<th><code>mtls</code></th>
					<th>Effect (Istio)</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>&quot;&quot;</code> (default)</td>
					<td>Defers to the mesh&rsquo;s own default (mesh-wide <code>PeerAuthentication</code> / <code>MeshConfig</code>) — no <code>PeerAuthentication</code> is rendered</td>
			</tr>
			<tr>
					<td><code>permissive</code></td>
					<td>Renders a <code>PeerAuthentication</code> accepting both mTLS and plaintext</td>
			</tr>
			<tr>
					<td><code>strict</code></td>
					<td>Requires mTLS on the workload&rsquo;s ports, <strong>except</strong> the webhook and health ports, which get a port-level <code>PERMISSIVE</code> carve-out</td>
			</tr>
	</tbody>
</table>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">istio</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">mtls</span><span class="p">:</span><span class="w"> </span><span class="l">strict</span><span class="w">
</span></span></span></code></pre></div><p>Under <code>strict</code>, every mesh-reachable port enforces mTLS while the apiserver and
kubelet still reach the webhook and probes over plaintext via the carve-out above.</p>
<h2 id="default-deny">Default-deny</h2>
<p><code>serviceMesh.defaultDeny.enabled</code> (default <code>false</code>) additionally renders a
namespace-wide default-deny so every pod in the install namespace rejects
unauthorized mesh traffic and the per-workload allows become the only exceptions (a
zero-trust namespace).</p>
<ul>
<li><strong>Istio</strong> renders an empty-spec <code>AuthorizationPolicy</code> (deny-all) scoped to the
whole namespace; it sits at lower precedence than the workload <code>ALLOW</code>, so the
per-port allows always win for the controller pod while everything else is denied.</li>
<li><strong>Linkerd</strong> has no per-object deny-all; the namespace default is set via the
<code>config.linkerd.io/default-inbound-policy</code> annotation, stamped onto the
chart-managed Namespace (requires <code>namespace.create=true</code>) — otherwise annotate the
namespace out of band.</li>
</ul>
<p>Enable this <strong>only when the controller owns its namespace</strong>, because the deny-all
also denies every co-located workload that does not have its own allowing
authorization.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">istio</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">defaultDeny</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">   </span><span class="c"># only when the controller owns this namespace</span><span class="w">
</span></span></span></code></pre></div><h2 id="native-passthrough">Native passthrough</h2>
<p>Anything the per-port <code>from</code> knobs cannot express goes into the engine&rsquo;s native
passthrough list, merged verbatim into the rendered objects.</p>
<p>Under the <code>istio</code> engine, <code>serviceMesh.istio.rules</code> are merged into the
<code>AuthorizationPolicy</code>&rsquo;s <code>spec.rules</code> (the <code>security.istio.io/v1</code> rule schema) — use it
for path/method matchers, <code>when</code> JWT-claim conditions, <code>ipBlocks</code>, and similar:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">istio</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">istio</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">rules</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">from</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">source</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">namespaces</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span>- <span class="l">flagger-system</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">to</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span>- <span class="nt">operation</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">methods</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span>- <span class="l">GET</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">paths</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span>- <span class="l">/gate/*</span><span class="w">
</span></span></span></code></pre></div><p>Under the <code>linkerd</code> engine, <code>serviceMesh.linkerd.authorizations</code> are appended verbatim
as additional documents after the rendered <code>Server</code> / <code>AuthorizationPolicy</code> /
<code>MeshTLSAuthentication</code> set — each entry must be a complete <code>policy.linkerd.io</code> object:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">serviceMesh</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">engine</span><span class="p">:</span><span class="w"> </span><span class="l">linkerd</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">linkerd</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">authorizations</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">policy.linkerd.io/v1beta3</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">AuthorizationPolicy</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">stageset-extra</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">targetRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">group</span><span class="p">:</span><span class="w"> </span><span class="l">policy.linkerd.io</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">Server</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">stageset-gate</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">requiredAuthenticationRefs</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span>- <span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ServiceAccount</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">flagger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">              </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">flagger-system</span><span class="w">
</span></span></span></code></pre></div><h2 id="which-traffic-gets-authorized">Which traffic gets authorized</h2>
<p>The mesh authorizes only meshed callers on the mesh-reachable ports; the non-mesh
ports stay open by design so the control plane keeps working.</p>
<table>
	<thead>
			<tr>
					<th>Port</th>
					<th>Authorized by the mesh?</th>
					<th>Why</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Gate (<code>8082</code>)</td>
					<td>Yes — <code>serviceMesh.gate.from</code></td>
					<td>Meshed Flagger polling the gate endpoint</td>
			</tr>
			<tr>
					<td>Metrics (<code>8080</code>)</td>
					<td>Yes — <code>serviceMesh.metrics.from</code></td>
					<td>Meshed Prometheus scrapers</td>
			</tr>
			<tr>
					<td>Webhook (<code>9443</code>)</td>
					<td>No — open carve-out</td>
					<td>The kube-apiserver is not in the mesh</td>
			</tr>
			<tr>
					<td>Health probes (<code>8081</code>)</td>
					<td>No — open carve-out</td>
					<td>The kubelet is not in the mesh</td>
			</tr>
	</tbody>
</table>
<p>For the full set of chart values, see
<a href="/reference/helm-values/">Helm chart values</a>
.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/service-mesh" term="service-mesh" label="service-mesh"/><category scheme="https://stageset.projects.metio.wtf/tags/istio" term="istio" label="istio"/><category scheme="https://stageset.projects.metio.wtf/tags/linkerd" term="linkerd" label="linkerd"/><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/><category scheme="https://stageset.projects.metio.wtf/tags/networking" term="networking" label="networking"/></entry></feed>