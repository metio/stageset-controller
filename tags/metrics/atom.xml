<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Metrics on StageSet Controller</title><link href="https://stageset.projects.metio.wtf/tags/metrics/" rel="alternate" type="text/html" title="html"/><link href="https://stageset.projects.metio.wtf/tags/metrics/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://stageset.projects.metio.wtf/tags/metrics/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-01T08:41:18+00:00</updated><author><name>metio.wtf</name></author><id>https://stageset.projects.metio.wtf/tags/metrics/</id><entry><title type="html">BudgetSourceUnavailable</title><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetexhausted/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetExhausted"/><link href="https://stageset.projects.metio.wtf/gating/error-budget/?utm_source=atom_feed" rel="related" type="text/html" title="Error-budget freeze"/><link href="https://stageset.projects.metio.wtf/runbooks/promotionblocked/?utm_source=atom_feed" rel="related" type="text/html" title="PromotionBlocked"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><id>https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>A metric source for an error-budget freeze or a promotion analysis could not be read.</blockquote><h2 id="symptom">Symptom</h2>
<p>A <code>BudgetSourceUnavailable</code> Warning event, the <code>stageset_metric_source_errors_total</code> counter rising, and — when a gate&rsquo;s <code>onSourceError</code> is <code>Hold</code> — <code>READY=False</code>, <code>REASON=BudgetSourceUnavailable</code>. With <code>onSourceError: Allow</code> the rollout proceeds and only the event and metric fire, so the condition may not show the reason.</p>
<h2 id="cause">Cause</h2>
<p>A metric source (the <a href="/gating/error-budget/">error-budget freeze</a>
 or a <a href="/gating/stage-promotion/">promotion analysis</a>
 check) was unreachable or returned no usable scalar: the Prometheus endpoint was down or returned a non-2xx, the query evaluated to an error, an empty or multi-sample vector, or <code>NaN</code> (the shape a wrong query takes). This is treated as <em>transient</em> — the controller keeps retrying.</p>
<p>What happens to the rollout depends on the gate&rsquo;s <code>onSourceError</code>:</p>
<ul>
<li>The <strong>error-budget freeze</strong> defaults to <code>Allow</code> (fail-open): blocking it would stop every deploy, including the hotfix you need during the very outage that took the source down.</li>
<li>A <strong>promotion analysis</strong> defaults to <code>Hold</code> (fail-closed): holding only parks the rollout at the current healthy stage, so it is safe to refuse to advance while behavior can&rsquo;t be verified.</li>
</ul>
<h2 id="diagnosis">Diagnosis</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace &lt;namespace&gt; describe stageset &lt;name&gt;   <span class="c1"># the event names the source error</span>
</span></span><span class="line"><span class="cl">kubectl --namespace &lt;namespace&gt; get stageset &lt;name&gt; --output <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">&#39;{.status.stages[*].promotionState.lastAnalysis}&#39;</span>
</span></span></code></pre></div><p>Check the address and query in <code>spec.errorBudget.source</code> / <code>spec.stages[].promotion.analysis.checks[].source</code>. From a pod in the namespace, confirm the Prometheus endpoint is reachable and the query returns a single scalar:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">curl -s <span class="s1">&#39;&lt;address&gt;/api/v1/query?query=&lt;query&gt;&#39;</span>
</span></span></code></pre></div><p>A <code>NaN</code>/empty result means the query&rsquo;s labels match nothing — fix the query, not the gate. A connection failure points at NetworkPolicy or a wrong address. If a bearer token is configured, confirm the referenced Secret has a non-empty <code>token</code> key.</p>
<h2 id="remediation">Remediation</h2>
<p>Fix the source so the query returns a single numeric scalar. While debugging a freeze, <code>spec.errorBudget.dryRun: true</code> records what would happen without holding anything. If a service is genuinely safety-critical and you want it to stop deploying when its SLO source is down, set <code>spec.errorBudget.onSourceError: Hold</code> (accepting that a source outage then blocks its deploys).</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/><category scheme="https://stageset.projects.metio.wtf/tags/error-budget" term="error-budget" label="error-budget"/><category scheme="https://stageset.projects.metio.wtf/tags/slo" term="slo" label="slo"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/troubleshooting" term="troubleshooting" label="troubleshooting"/></entry><entry><title type="html">Error-budget freeze</title><link href="https://stageset.projects.metio.wtf/gating/error-budget/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetexhausted/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetExhausted"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/observability/slos/?utm_source=atom_feed" rel="related" type="text/html" title="Service level objectives"/><link href="https://stageset.projects.metio.wtf/observability/dashboard/?utm_source=atom_feed" rel="related" type="text/html" title="Dashboard"/><link href="https://stageset.projects.metio.wtf/observability/metrics/?utm_source=atom_feed" rel="related" type="text/html" title="Metrics"/><id>https://stageset.projects.metio.wtf/gating/error-budget/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Freeze new-revision rollouts while a service is out of its SLO error budget, resuming on its own when it recovers.</blockquote><p>An error-budget freeze holds new-revision rollouts while a service is out of its
SLO error budget, and resumes them on its own once the budget recovers — the
Google SRE error-budget policy: when the budget is spent, stop shipping feature
changes until reliability is back. The controller does no SLO math; it reads one
number (the remaining budget) from a metric source you already run and compares
it to a threshold.</p>
<h2 id="freeze-on-a-prometheus-query">Freeze on a Prometheus query</h2>
<p>Point <code>spec.errorBudget</code> at an instant query that returns the remaining budget as
a scalar — every SLO tool exposes this (Sloth records
<code>slo:period_error_budget_remaining:ratio</code> directly; Pyrra, Grafana SLO, and
Nobl9&rsquo;s Prometheus API yield it in one line):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">stages.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">StageSet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">checkout</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">apps</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceAccountName</span><span class="p">:</span><span class="w"> </span><span class="l">deployer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">errorBudget</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">source</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">prometheus</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">address</span><span class="p">:</span><span class="w"> </span><span class="l">http://prometheus.monitoring:9090</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">query</span><span class="p">:</span><span class="w"> </span><span class="l">slo:period_error_budget_remaining:ratio{sloth_service=&#34;checkout&#34;,sloth_slo=&#34;availability&#34;}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">freezeThreshold</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0&#34;</span><span class="w">      </span><span class="c"># freeze when remaining &lt; this; &#34;0&#34; = only when overspent</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">resumeThreshold</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0.05&#34;</span><span class="w">   </span><span class="c"># resume only once it recovers to here (hysteresis)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l">5m             </span><span class="w"> </span><span class="c"># re-check cadence while frozen; defaults to spec.interval</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">checkout-prod</span><span class="w">
</span></span></span></code></pre></div><p>While the remaining budget is below <code>freezeThreshold</code>, the StageSet reports
<code>Ready=False</code> with reason <code>BudgetExhausted</code> (an already-deployed StageSet stays
<code>Ready=True</code> and records the freeze on <code>status.budgetFreeze</code>). The current
revision keeps having its drift corrected — a frozen service still gets its
declared state enforced — and only new-revision rollouts wait. The controller
re-checks every <code>interval</code> and advances on its own once the budget reaches
<code>resumeThreshold</code>.</p>
<p><code>freezeThreshold</code> is required and has no default. <code>resumeThreshold</code> defaults to
<code>freezeThreshold</code> (no hysteresis); set it higher to stop a budget hovering at the
line from flapping the freeze. <code>interval</code> defaults to the StageSet&rsquo;s reconcile
interval.</p>
<h2 id="it-composes-with-update-windows">It composes with update windows</h2>
<p><code>spec.errorBudget</code> and <a href="/gating/update-windows/"><code>spec.updateWindows</code></a>
 are combined
under a logical AND: a new revision rolls out only if the update window is open <strong>and</strong> the budget is
healthy. A closed window holds the rollout even when the budget is fine (and the
budget source isn&rsquo;t even queried), and an exhausted budget holds it even inside
an open window. Use both to deploy only during a maintenance window <em>and</em> only
while in budget.</p>
<h2 id="authenticating-the-query">Authenticating the query</h2>
<p>When Prometheus needs a bearer token, reference a Secret in the StageSet&rsquo;s
namespace with the token under the <code>token</code> key:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="w">  </span><span class="nt">errorBudget</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">source</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">prometheus</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">address</span><span class="p">:</span><span class="w"> </span><span class="l">https://prometheus.monitoring:9090</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">query</span><span class="p">:</span><span class="w"> </span><span class="l">slo:period_error_budget_remaining:ratio{sloth_service=&#34;checkout&#34;}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prometheus-auth</span><span class="w">
</span></span></span></code></pre></div><p>The source address is dialed with the same SSRF guard as HTTP actions: loopback,
link-local, cloud-metadata, multicast, and unspecified addresses are refused,
while in-cluster private addresses (where Prometheus usually lives) are allowed.</p>
<h2 id="reading-from-a-saas-slo-api-webhook">Reading from a SaaS SLO API (webhook)</h2>
<p>When the budget lives in a SaaS SLO platform with no Prometheus endpoint (Nobl9,
Grafana Cloud), use a <code>webhook</code> source instead: the controller GETs a JSON
document and extracts the scalar with a kubectl-style JSONPath.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="w">  </span><span class="nt">errorBudget</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">source</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">url</span><span class="p">:</span><span class="w"> </span><span class="l">https://nobl9.example/api/v2/slos/shop/checkout</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">jsonPath</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;{.objectives[0].errorBudgetRemaining}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">secretRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">nobl9-token       </span><span class="w"> </span><span class="c"># optional bearer token, under the &#34;token&#34; key</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">freezeThreshold</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0&#34;</span><span class="w">
</span></span></span></code></pre></div><p>A source is exactly one of <code>prometheus</code> or <code>webhook</code>. The <code>jsonPath</code> must resolve
to a single numeric (or numeric-string) value; anything else (no match, multiple
matches, an object, <code>NaN</code>) is treated as an unreadable source and routed through
<code>onSourceError</code>. The webhook URL is dialed through the same SSRF guard.</p>
<h2 id="freezing-a-single-stage">Freezing a single stage</h2>
<p><code>spec.errorBudget</code> freezes the whole rollout. To freeze only one stage on its own
SLO — e.g. hold <code>prod</code> while <code>prod</code>&rsquo;s budget is spent, but let <code>staging</code> keep
rolling — put an <code>errorBudget</code> on that stage:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">staging</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">web-staging</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">web-prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">errorBudget</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">source</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">prometheus</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">address</span><span class="p">:</span><span class="w"> </span><span class="l">http://prometheus.monitoring:9090</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="nt">query</span><span class="p">:</span><span class="w"> </span><span class="l">slo:period_error_budget_remaining:ratio{sloth_service=&#34;checkout-prod&#34;}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">freezeThreshold</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0.1&#34;</span><span class="w">
</span></span></span></code></pre></div><p>A per-stage budget gates <strong>entry</strong> to the stage: while it is exhausted, a new
revision is held from rolling into that stage (earlier stages keep rolling, and
the stage&rsquo;s currently-applied revision keeps having its drift corrected). It takes
the same fields as the rollout-wide budget (<code>freezeThreshold</code>/<code>resumeThreshold</code>
hysteresis, <code>interval</code>, <code>onSourceError</code>, <code>dryRun</code>) and the same
<a href="#shipping-a-reliability-fix-while-frozen"><code>budget-override</code></a>
 break-glass clears
both. This complements a <a href="/gating/stage-promotion/">promotion analysis</a>
, which
gates <em>exit</em> (advancing past a stage once it has applied).</p>
<h2 id="when-the-source-is-unreachable">When the source is unreachable</h2>
<p><code>onSourceError</code> decides what happens when the query can&rsquo;t be read (Prometheus
down, a non-2xx, an empty result, <code>NaN</code>):</p>
<ul>
<li><code>Allow</code> (default) — proceed. Blocking a rollout-wide freeze would stop <em>every</em>
deploy, including the hotfix you need during the very outage that took the
source down, so the freeze fails open. The error is still loud: a
<code>BudgetSourceUnavailable</code> Warning event and the <code>stageset_metric_source_errors_total</code>
metric. See the <a href="/runbooks/budgetsourceunavailable/">BudgetSourceUnavailable runbook</a>
.</li>
<li><code>Hold</code> — block. Set this for a service that must stop deploying when its SLO
source is down, accepting that a source outage then blocks its rollouts.</li>
</ul>
<p>A misconfigured query that always returns <code>NaN</code> would silently fail open, so set
<code>dryRun: true</code> to prove a freeze rule fires before it gates — it records what
<em>would</em> freeze on <code>status.budgetFreeze</code> and the metrics, without holding
anything.</p>
<h2 id="shipping-a-reliability-fix-while-frozen">Shipping a reliability fix while frozen</h2>
<p>The error-budget policy explicitly exempts reliability and security fixes. Break
the glass to apply a held rollout once, without disabling the gate:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">stagesetctl reconcile checkout --namespace apps --budget-override
</span></span></code></pre></div><h2 id="what-it-observes">What it observes</h2>
<p>The freeze reads <em>remaining budget</em>, not <em>burn rate</em> — a fast-burning but
not-yet-exhausted service still rolls out. Multi-window burn-rate alerting is the
SLO tool&rsquo;s job. To gate on observed behavior per stage (error rate, latency,
burn rate) rather than rollout-wide, use a
<a href="/gating/stage-promotion/">promotion analysis</a>
, which shares this same metric
source contract.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/error-budget" term="error-budget" label="error-budget"/><category scheme="https://stageset.projects.metio.wtf/tags/slo" term="slo" label="slo"/><category scheme="https://stageset.projects.metio.wtf/tags/scheduling" term="scheduling" label="scheduling"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/></entry><entry><title type="html">Metrics</title><link href="https://stageset.projects.metio.wtf/observability/metrics/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="related" type="text/html" title="Alerting"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/gating/error-budget/?utm_source=atom_feed" rel="related" type="text/html" title="Error-budget freeze"/><link href="https://stageset.projects.metio.wtf/observability/logging/?utm_source=atom_feed" rel="related" type="text/html" title="Logging"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><id>https://stageset.projects.metio.wtf/observability/metrics/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The controller&rsquo;s Prometheus metrics — the stageset_* custom series and controller-runtime signals, scraping with a ServiceMonitor or a plain config, querying with PromQL, and the chart values that drive them.</blockquote><p>The controller exports Prometheus metrics on an HTTP endpoint: a set of custom
<code>stageset_*</code> series describing reconcile outcomes, stage applies, drift
correction, deferred rollouts, webhook-cert renewals, and per-stage readiness —
alongside the controller-runtime and workqueue series every controller-runtime
manager exposes for free.</p>
<h2 id="the-controller-binary">The controller binary</h2>
<p>The controller binds its metrics endpoint via <code>--metrics-bind-address</code>, defaulting
to <code>:8080</code>. Setting it to <code>0</code> disables the endpoint. Every custom metric is
registered against controller-runtime&rsquo;s registry, so it rides the same endpoint as
the built-in series.</p>
<p>All counters and the readiness gauge are labelled by <code>namespace</code> and the StageSet
<code>name</code> so a single StageSet&rsquo;s behaviour is isolatable.</p>
<h3 id="reconcile-outcomes">Reconcile outcomes</h3>
<table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Type</th>
					<th>Labels</th>
					<th>Meaning</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>stageset_reconcile_total</code></td>
					<td>Counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>reason</code></td>
					<td>Reconciles by their terminal Ready-condition reason. The <code>reason</code> label is one of the wire-stable Ready reasons.</td>
			</tr>
			<tr>
					<td><code>stageset_update_deferred_total</code></td>
					<td>Counter</td>
					<td><code>namespace</code>, <code>name</code></td>
					<td>Reconciles that held a rollout because an update window was closed.</td>
			</tr>
	</tbody>
</table>
<h3 id="stage-progress">Stage progress</h3>
<table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Type</th>
					<th>Labels</th>
					<th>Meaning</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>stageset_stage_applied_total</code></td>
					<td>Counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Stages that applied and verified successfully.</td>
			</tr>
			<tr>
					<td><code>stageset_drift_corrected_total</code></td>
					<td>Counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Objects whose out-of-band drift the apply corrected on a steady-state reconcile (same revision as last applied).</td>
			</tr>
			<tr>
					<td><code>stageset_stage_ready</code></td>
					<td>Gauge</td>
					<td><code>namespace</code>, <code>stageset</code>, <code>stage</code></td>
					<td>Whether a stage is Ready (<code>1</code>) or not (<code>0</code>) at the current observed state. A metrics-gated progressive-delivery controller — Argo Rollouts&rsquo; Prometheus metric provider, for example — can hold a rollout directly on this gauge. The series is deleted when a StageSet or one of its stages goes away, so a removed stage leaves no stale gauge.</td>
			</tr>
	</tbody>
</table>
<h3 id="webhook">Webhook</h3>
<table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Type</th>
					<th>Labels</th>
					<th>Meaning</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>stageset_webhook_cert_renewal_failures_total</code></td>
					<td>Counter</td>
					<td>—</td>
					<td>Failed self-signed webhook certificate renewals in the background renewer goroutine. Meaningful only in self-signed webhook mode.</td>
			</tr>
	</tbody>
</table>
<h3 id="controller-runtime-and-workqueue-metrics">Controller-runtime and workqueue metrics</h3>
<p>The manager also exports the standard controller-runtime series without any extra
configuration, all carrying <code>controller=&quot;stageset&quot;</code>:</p>
<ul>
<li><code>controller_runtime_reconcile_total</code>, <code>controller_runtime_reconcile_errors_total</code>,
and the <code>controller_runtime_reconcile_time_seconds</code> histogram.</li>
<li><code>workqueue_depth</code>, <code>workqueue_adds_total</code>, the <code>workqueue_queue_duration_seconds</code>
and <code>workqueue_work_duration_seconds</code> histograms, and the retry counters.</li>
</ul>
<h3 id="querying-with-promql">Querying with PromQL</h3>
<p>A few starting points once the series are scraped:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-promql" data-lang="promql"><span class="line"><span class="cl"><span class="c1"># Reconciles settling to a non-happy reason, per StageSet, over 5m.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">sum</span><span class="w"> </span><span class="k">by</span><span class="w"> </span><span class="o">(</span><span class="nv">namespace</span><span class="p">,</span><span class="w"> </span><span class="nv">name</span><span class="p">,</span><span class="w"> </span><span class="nv">reason</span><span class="o">)</span><span class="w"> </span><span class="o">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="kr">rate</span><span class="o">(</span><span class="nv">stageset_reconcile_total</span><span class="p">{</span><span class="nl">reason</span><span class="o">!~</span><span class="p">&#34;</span><span class="s">Succeeded|Suspended</span><span class="p">&#34;}[</span><span class="s">5m</span><span class="p">]</span><span class="o">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c1"># Stages currently not Ready.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nv">stageset_stage_ready</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="mi">0</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c1"># Reconcile p99 latency for the controller.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="kr">histogram_quantile</span><span class="o">(</span><span class="mf">0.99</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="k">sum</span><span class="w"> </span><span class="k">by</span><span class="w"> </span><span class="o">(</span><span class="nv">le</span><span class="o">)</span><span class="w"> </span><span class="o">(</span><span class="kr">rate</span><span class="o">(</span><span class="nv">controller_runtime_reconcile_time_seconds_bucket</span><span class="p">{</span><span class="nl">controller</span><span class="o">=</span><span class="p">&#34;</span><span class="s">stageset</span><span class="p">&#34;}[</span><span class="s">10m</span><span class="p">]</span><span class="o">))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">)</span><span class="w">
</span></span></span></code></pre></div><p>The <a href="/observability/alerting/">alerting</a>
 catalog builds on exactly these expressions.</p>
<h2 id="the-helm-chart">The Helm chart</h2>
<p>The chart binds the metrics port through <code>ports.metrics</code> (default <code>8080</code>) and
always renders a ClusterIP <code>Service</code> named <code>&lt;release&gt;-metrics</code> exposing a <code>metrics</code>
port that targets the container&rsquo;s <code>metrics</code> port, so any scrape mechanism has a
stable target.</p>
<h3 id="scrape-with-a-servicemonitor">Scrape with a ServiceMonitor</h3>
<p>When the Prometheus operator is installed, opt into a <code>ServiceMonitor</code> that
selects the metrics Service:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l">30s</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># Extra labels so your Prometheus instance&#39;s serviceMonitorSelector picks it up.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">additionalLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">release</span><span class="p">:</span><span class="w"> </span><span class="l">kube-prometheus-stack</span><span class="w">
</span></span></span></code></pre></div><h3 id="scrape-without-the-prometheus-operator">Scrape without the Prometheus operator</h3>
<p>Without the operator&rsquo;s CRDs, point a plain Prometheus scrape config at the metrics
Service, or annotate the rendered Service for an annotation-driven scrape discovery
setup:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl annotate service &lt;release&gt;-metrics <span class="se">\
</span></span></span><span class="line"><span class="cl">  prometheus.io/scrape<span class="o">=</span><span class="nb">true</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  prometheus.io/port<span class="o">=</span><span class="m">8080</span>
</span></span></code></pre></div><p>For the full flag list with defaults, see the
<a href="/reference/configuration/">configuration reference</a>
.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/observability" term="observability" label="observability"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/prometheus" term="prometheus" label="prometheus"/><category scheme="https://stageset.projects.metio.wtf/tags/promql" term="promql" label="promql"/></entry><entry><title type="html">Observability</title><link href="https://stageset.projects.metio.wtf/observability/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="related" type="text/html" title="Alerting"/><link href="https://stageset.projects.metio.wtf/observability/logging/?utm_source=atom_feed" rel="related" type="text/html" title="Logging"/><link href="https://stageset.projects.metio.wtf/observability/metrics/?utm_source=atom_feed" rel="related" type="text/html" title="Metrics"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><id>https://stageset.projects.metio.wtf/observability/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The four observability pillars for the controller — structured logging, OTLP tracing, Prometheus metrics, and the opt-in alert catalog — plus service level objectives and a ready-made dashboard.</blockquote><blockquote>
<p><strong>Status Conditions and Kubernetes Events are the primary status surface</strong> —
<code>kubectl describe</code>, <code>kubectl wait --for=condition</code>, and Flux&rsquo;s
notification-controller all read them. The four pillars below are additive
operator-side detail; they explain how a reconcile behaved, but the condition
on the StageSet is what tells you whether it is Ready.</p>
</blockquote>
<p>The controller surfaces its behaviour through four pillars, each configured by a
small set of binary flags that the Helm chart drives from values:</p>
<ul>
<li><strong><a href="/observability/logging/">Logging</a>
</strong> — structured <code>log/slog</code> output (JSON or text)
for the controller and, through the logr bridge, controller-runtime itself.
Read it with <code>kubectl logs</code> and filter with <code>jq</code>.</li>
<li><strong><a href="/observability/tracing/">Tracing</a>
</strong> — OpenTelemetry spans exported over OTLP gRPC to
a collector you point the controller at. Off until you set an endpoint.</li>
<li><strong><a href="/observability/metrics/">Metrics</a>
</strong> — a <code>stageset_*</code> family of Prometheus series on
reconcile outcomes, stage applies, drift correction, deferred rollouts, and
per-stage readiness, alongside the controller-runtime and workqueue series.
Scrape with a <code>ServiceMonitor</code> or a plain scrape config and query with PromQL.</li>
<li><strong><a href="/observability/alerting/">Alerting</a>
</strong> — an opt-in <code>PrometheusRule</code> with a starter
alert set whose thresholds are tunable values, plus the Kubernetes Events the
controller emits on StageSet transitions. Every alert links to a
<a href="/runbooks/">runbook</a>
.</li>
</ul>
<p>Two further pages build on the metrics:</p>
<ul>
<li><strong><a href="/observability/slos/">Service level objectives</a>
</strong> — the controller&rsquo;s own
reconcile-availability and reconcile-latency SLOs, computed from the
<code>stageset_*</code> series, with their targets and error budgets.</li>
<li><strong><a href="/observability/dashboard/">Dashboard</a>
</strong> — a ready-made Grafana dashboard
over the controller and workqueue series.</li>
</ul>
<p>For each pillar you configure the controller binary&rsquo;s flags, read or query its
output, and drive both from the Helm chart values. The full flag list with
defaults is on the <a href="/reference/configuration/">configuration reference</a>
.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/observability" term="observability" label="observability"/><category scheme="https://stageset.projects.metio.wtf/tags/logging" term="logging" label="logging"/><category scheme="https://stageset.projects.metio.wtf/tags/tracing" term="tracing" label="tracing"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/slos" term="slos" label="slos"/><category scheme="https://stageset.projects.metio.wtf/tags/dashboard" term="dashboard" label="dashboard"/></entry><entry><title type="html">Operations</title><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><link href="https://stageset.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="related" type="text/html" title="Alerting"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><id>https://stageset.projects.metio.wtf/running/operations/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Metrics, alerts, events, and runbooks for running the controller day to day.</blockquote><h2 id="metrics">Metrics</h2>
<p>The controller registers custom metrics on the controller-runtime registry, served
on <code>--metrics-bind-address</code> (<code>:8080</code>) alongside the standard
<code>controller_runtime_*</code> and <code>workqueue_*</code> series. Enable scraping with the chart&rsquo;s
opt-in <code>ServiceMonitor</code> (<code>metrics.serviceMonitor.enabled</code>):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># values.yaml</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">        </span><span class="c"># needs the Prometheus operator CRDs</span><span class="w">
</span></span></span></code></pre></div><table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Type</th>
					<th>Labels</th>
					<th>Meaning</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>stageset_reconcile_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>reason</code></td>
					<td>Reconciles, by terminal Ready reason.</td>
			</tr>
			<tr>
					<td><code>stageset_stage_applied_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Stages applied and verified.</td>
			</tr>
			<tr>
					<td><code>stageset_drift_corrected_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Out-of-band drift re-asserted on a steady-state reconcile.</td>
			</tr>
			<tr>
					<td><code>stageset_update_deferred_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code></td>
					<td>Rollouts held by a closed update window.</td>
			</tr>
			<tr>
					<td><code>stageset_webhook_cert_renewal_failures_total</code></td>
					<td>counter</td>
					<td><em>(none)</em></td>
					<td>Failed self-signed webhook cert renewals.</td>
			</tr>
			<tr>
					<td><code>stageset_teardown_force_drop_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code></td>
					<td>Finalizers force-dropped after <code>--max-teardown-wait</code> of failing teardown; sustained non-zero values flag an unreachable target and orphaned objects. See <a href="/runbooks/teardown-forced/">TeardownForced</a>
.</td>
			</tr>
			<tr>
					<td><code>stageset_stage_ready</code></td>
					<td>gauge</td>
					<td><code>namespace</code>, <code>stageset</code>, <code>stage</code></td>
					<td><code>1</code> when a stage is Ready, else <code>0</code> — for metric-based <a href="/guides/progressive-delivery/#argo-rollouts">progressive delivery</a>
.</td>
			</tr>
	</tbody>
</table>
<h2 id="alerts">Alerts</h2>
<p>The chart ships an opt-in <code>PrometheusRule</code> with a starter alert set, gated on
<code>metrics.prometheusRule.enabled</code> (requires the
<a href="https://prometheus-operator.dev/">Prometheus operator</a>
 CRDs). It covers the
custom <code>stageset_*</code> metrics plus controller-runtime signals:</p>
<table>
	<thead>
			<tr>
					<th>Alert</th>
					<th>Fires on</th>
					<th>Severity</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>StageSetReconcileErrorsHigh</code></td>
					<td>per-StageSet Ready=False rate (excludes the healthy <code>Succeeded</code>/<code>Suspended</code> reasons)</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetControllerWorkqueueDepthHigh</code></td>
					<td>the reconcile queue not draining</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetReconcileLatencyHigh</code></td>
					<td>reconcile p99 latency over threshold</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetControllerPodDown</code></td>
					<td>a controller pod NotReady</td>
					<td>critical</td>
			</tr>
			<tr>
					<td><code>StageSetWebhookCertRenewalFailing</code></td>
					<td>self-signed cert rotation failing</td>
					<td>critical</td>
			</tr>
	</tbody>
</table>
<p>Every threshold is a knob under <code>metrics.prometheusRule.thresholds</code>, and
<code>extraAlertLabels</code> is merged onto every rendered alert so all stageset alerts can
route through one Alertmanager receiver. Each alert carries a <code>runbook_url</code>
annotation pointing at the matching <a href="/runbooks/">runbook</a>
 page on this site; the
URL prefix is fixed to this site, and the reconcile-errors alert templates the
URL on <code>$labels.reason</code>. Append your own rules under
<code>metrics.prometheusRule.extraRules</code>, and silence a built-in alert by raising its
threshold rather than forking the chart. When <code>StageSetControllerWorkqueueDepthHigh</code>
or <code>StageSetReconcileLatencyHigh</code> fires persistently, the controller is at capacity
— see <a href="/running/scale-and-capacity/">Scale and capacity</a>
 for which lever to
pull.</p>
<h2 id="events">Events</h2>
<p>The controller emits Kubernetes Events on every Ready-condition transition, so
<code>kubectl describe stageset &lt;name&gt;</code> and <a href="https://fluxcd.io/">Flux</a>
&rsquo;s
<code>notification-controller</code> (via an <code>Alert</code> targeting <code>kind: StageSet</code>) both
surface what happened. Normal events
include <code>Succeeded</code>, <code>UpdateDeferred</code>, <code>MigrationStarted</code>, and
<code>MigrationCompleted</code>; warnings include <code>StageFailed</code>, <code>DriftCorrected</code>,
<code>RolledBack</code>, <code>MigrationFailed</code>, <code>OnFailureAction</code>, <code>RollbackStoreFailed</code>, and
<code>TeardownForced</code> (a deleting StageSet&rsquo;s finalizer force-dropped after
<code>--max-teardown-wait</code> — see <a href="/runbooks/teardown-forced/">TeardownForced</a>
).</p>
<h2 id="runbooks">Runbooks</h2>
<p>Every actionable Ready-condition reason has a <a href="/runbooks/">runbook</a>
 covering the
symptom, cause, diagnosis, and remediation. The controller appends a
<code>(runbook: https://stageset.projects.metio.wtf/runbooks/&lt;reason&gt;/)</code> link to every
actionable Ready message (the reason lower-cased into a path segment), so a
<code>kubectl describe</code> links straight to the fix. Healthy reasons (<code>Succeeded</code>,
<code>Suspended</code>) get no link.</p>
<p>For example, a <code>StageFailed</code> StageSet shows:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">Message:  stage &#34;application&#34; failed: … (runbook: https://stageset.projects.metio.wtf/runbooks/stagefailed/)
</span></span></code></pre></div><h2 id="forcing-a-reconcile">Forcing a reconcile</h2>
<p>The controller reconciles on its <code>spec.interval</code>, on source changes, and on
demand. To trigger an out-of-band run, stamp the standard annotation — which is
what <code>flux reconcile</code> and <a href="/cli/reconcile/"><code>stagesetctl reconcile</code></a>
 do for you:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl annotate stageset my-app <span class="se">\
</span></span></span><span class="line"><span class="cl">  reconcile.fluxcd.io/requestedAt<span class="o">=</span><span class="s2">&#34;</span><span class="k">$(</span>date -u +%FT%TZ<span class="k">)</span><span class="s2">&#34;</span> --overwrite
</span></span></code></pre></div><p>The handled token is recorded in <code>status.lastHandledReconcileAt</code>.</p>
<h2 id="drift-correction">Drift correction</h2>
<p>On a steady-state reconcile the controller re-asserts the desired state, healing
out-of-band changes to managed objects. Each correction emits a <code>DriftCorrected</code>
event and increments <code>stageset_drift_corrected_total</code>. Tighten the cadence with
<code>spec.driftDetectionInterval</code> when you need faster healing than <code>spec.interval</code>.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/operations" term="operations" label="operations"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/></entry><entry><title type="html">PromotionBlocked</title><link href="https://stageset.projects.metio.wtf/runbooks/promotionblocked/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/awaitingpromotion/?utm_source=atom_feed" rel="related" type="text/html" title="AwaitingPromotion"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/soaking/?utm_source=atom_feed" rel="related" type="text/html" title="Soaking"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><id>https://stageset.projects.metio.wtf/runbooks/promotionblocked/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>A stage&rsquo;s promotion analysis breached its thresholds, so the rollout will not advance past it.</blockquote><h2 id="symptom">Symptom</h2>
<p><code>READY=False</code>, <code>REASON=PromotionBlocked</code>. <code>status.stages[].promotionState</code> shows <code>phase: Blocked</code>, an <code>analysisFailures</code> count, and <code>lastAnalysis</code> with each check&rsquo;s observed value and verdict. When the stage&rsquo;s <code>onFailure</code> is <code>Rollback</code>, a Warning event records the revert and <code>promotionState.abortedRevision</code> names the revision that was rolled back.</p>
<h2 id="cause">Cause</h2>
<p>A <a href="/gating/stage-promotion/">promotion analysis</a>
 on the named stage failed: one or more checks read a metric outside its threshold for more than <code>failureLimit</code> consecutive evaluations. The stage applied and became Ready, but its <em>observed behavior</em> — error rate, latency, SLO burn, whatever the checks measure — is out of bounds, so the rollout is not advanced to the next stage.</p>
<p><code>onFailure</code> decides what happened:</p>
<ul>
<li><code>Hold</code> (default): the stage stays applied but not promoted, surfacing why.</li>
<li><code>Rollback</code>: the stage was reverted to its last-known-good revision (scoped to this stage; earlier promoted stages are untouched) and the failing revision is parked so it is not re-applied each reconcile.</li>
</ul>
<h2 id="diagnosis">Diagnosis</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace &lt;namespace&gt; get stageset &lt;name&gt; --output <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">&#39;{.status.stages[*].promotionState.lastAnalysis}&#39;</span>
</span></span><span class="line"><span class="cl">kubectl --namespace &lt;namespace&gt; get stageset &lt;name&gt; --output <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">&#39;{.spec.stages[*].promotion.analysis}&#39;</span>
</span></span></code></pre></div><p><code>lastAnalysis.checks[]</code> shows each check&rsquo;s observed <code>value</code>, whether it was <code>ok</code>, and any source <code>error</code>. Confirm whether the metric reflects a real regression in the new revision or a too-tight threshold / wrong query. The <code>stageset_stage_promotion_blocked</code> gauge is <code>1</code> for a blocked stage.</p>
<h2 id="remediation">Remediation</h2>
<p>If the new revision is genuinely bad, fix it forward (push a corrected revision) — a new revision clears the failure count and re-runs the analysis. If the analysis itself is wrong (threshold too tight, query off), correct <code>spec.stages[].promotion.analysis</code> and re-reconcile. To accept the current revision despite the analysis (you have confirmed it is healthy), promote past the gate:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">stagesetctl promote &lt;name&gt; --namespace &lt;namespace&gt; --stage &lt;stage&gt;
</span></span></code></pre></div><p>While tuning a new analysis, set <code>dryRun: true</code> on it so it records what <em>would</em> block without holding or rolling back anything.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/><category scheme="https://stageset.projects.metio.wtf/tags/promotion" term="promotion" label="promotion"/><category scheme="https://stageset.projects.metio.wtf/tags/analysis" term="analysis" label="analysis"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/troubleshooting" term="troubleshooting" label="troubleshooting"/></entry><entry><title type="html">Reconcile latency high</title><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/runbooks/promotionblocked/?utm_source=atom_feed" rel="related" type="text/html" title="PromotionBlocked"/><id>https://stageset.projects.metio.wtf/runbooks/reconcile-latency/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Reconcile p99 latency for the StageSet controller is above threshold.</blockquote><h2 id="symptom">Symptom</h2>
<p><code>controller_runtime_reconcile_time_seconds</code> p99 for <code>controller=&quot;stageset&quot;</code> exceeds
the configured threshold; the <code>StageSetReconcileLatencyHigh</code> alert fires (see
<a href="/running/operations/">operations</a>
 for the alert set and its thresholds).</p>
<h2 id="cause">Cause</h2>
<p>A single reconcile does a lot of work — resolve and fetch every stage&rsquo;s artifact,
kustomize-build, server-side apply, prune, verify readiness, and run actions — all
impersonating the tenant <code>ServiceAccount</code>. Latency climbs when any of those is slow:</p>
<ul>
<li>large artifacts or slow artifact servers,</li>
<li>many objects per stage (apply + prune scale with object count),</li>
<li>readiness waits and <code>wait</code>/<code>http</code>/<code>job</code> actions with long timeouts,</li>
<li>apiserver or tenant-authorization slowness.</li>
</ul>
<h2 id="diagnosis">Diagnosis</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace stageset-system logs deploy/stageset-controller --tail<span class="o">=</span><span class="m">200</span> <span class="p">|</span> grep -i <span class="s1">&#39;slow\|timeout\|took&#39;</span>
</span></span></code></pre></div><p>Break the latency down by stage count and artifact size; a single StageSet with
many large stages dominates p99.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Split a very large StageSet into smaller ones, or fewer objects per stage.</li>
<li>Tighten action <code>timeout</code>s so a slow gate fails fast instead of stretching the
reconcile.</li>
<li>Raise <code>spec.interval</code> where freshness isn&rsquo;t critical.</li>
<li>Address upstream artifact-server or apiserver latency.</li>
</ul>
<p>If the queue itself is backing up, see <a href="/runbooks/workqueue-saturation/">workqueue saturation</a>
.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/troubleshooting" term="troubleshooting" label="troubleshooting"/></entry><entry><title type="html">Service level objectives</title><link href="https://stageset.projects.metio.wtf/observability/slos/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/observability/dashboard/?utm_source=atom_feed" rel="related" type="text/html" title="Dashboard"/><link href="https://stageset.projects.metio.wtf/gating/error-budget/?utm_source=atom_feed" rel="related" type="text/html" title="Error-budget freeze"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><id>https://stageset.projects.metio.wtf/observability/slos/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-19T09:16:22+02:00</updated><content type="html"><![CDATA[<blockquote>The StageSet controller&rsquo;s SLOs — reconcile availability and reconcile latency — with their SLIs, targets, error budget, the dashboard that shows them, and how to tune them.</blockquote><p>The StageSet controller tracks two service-level objectives. Each is an objective
on a service-level indicator (SLI) computed from the
<a href="/observability/metrics/">metrics</a>
, measured over a rolling window. The published
<a href="/observability/dashboard/">dashboard</a>
 renders them, and the Helm chart can alert
on them.</p>
<h2 id="slo-1--reconcile-availability">SLO 1 — reconcile availability</h2>
<p><strong>Objective:</strong> ≥ <strong>99%</strong> of syncing reconciles reach <code>Ready=True</code> (reason
<code>Succeeded</code>), over a <strong>28-day</strong> window.</p>
<p>The SLI counts only reconciles that were actually trying to sync. The intentional
and upstream-waiting reasons are excluded from the failure half — a paused
StageSet (<code>Suspended</code>), a rollout held by an update window (<code>UpdateDeferred</code>), and
a wait on an upstream artifact or dependency (<code>SourceNotReady</code>,
<code>DependencyNotReady</code>) are not failures and must not burn the budget:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-promql" data-lang="promql"><span class="line"><span class="cl"><span class="k">sum</span><span class="o">(</span><span class="kr">rate</span><span class="o">(</span><span class="nv">stageset_reconcile_total</span><span class="p">{</span><span class="nl">reason</span><span class="o">=</span><span class="p">&#34;</span><span class="s">Succeeded</span><span class="p">&#34;}[</span><span class="s">28d</span><span class="p">]</span><span class="o">))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">/</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="k">sum</span><span class="o">(</span><span class="kr">rate</span><span class="o">(</span><span class="nv">stageset_reconcile_total</span><span class="p">{</span><span class="nl">reason</span><span class="o">=</span><span class="p">&#34;</span><span class="s">Succeeded</span><span class="p">&#34;}[</span><span class="s">28d</span><span class="p">]</span><span class="o">))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="o">+</span><span class="w"> </span><span class="k">sum</span><span class="o">(</span><span class="kr">rate</span><span class="o">(</span><span class="nv">stageset_reconcile_total</span><span class="p">{</span><span class="nl">reason</span><span class="o">!~</span><span class="p">&#34;</span><span class="s">Succeeded|Suspended|UpdateDeferred|SourceNotReady|DependencyNotReady</span><span class="p">&#34;}[</span><span class="s">28d</span><span class="p">]</span><span class="o">))</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">)</span><span class="w">
</span></span></span></code></pre></div><p>The <strong>error budget</strong> is the 1% of reconciles allowed to fail over the window.
Remaining budget, normalised so <code>1</code> is full and <code>0</code> is exhausted:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-promql" data-lang="promql"><span class="line"><span class="cl"><span class="o">(&lt;</span><span class="nv">availability</span><span class="o">&gt;</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="mf">0.99</span><span class="o">)</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="o">(</span><span class="mi">1</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="mf">0.99</span><span class="o">)</span><span class="w">
</span></span></span></code></pre></div><h2 id="slo-2--reconcile-latency">SLO 2 — reconcile latency</h2>
<p><strong>Objective:</strong> the StageSet controller&rsquo;s <strong>p95</strong> reconcile duration stays
<strong>below 30s</strong> over the window.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-promql" data-lang="promql"><span class="line"><span class="cl"><span class="kr">histogram_quantile</span><span class="o">(</span><span class="mf">0.95</span><span class="p">,</span><span class="w"> </span><span class="k">sum</span><span class="w"> </span><span class="k">by</span><span class="w"> </span><span class="o">(</span><span class="nv">le</span><span class="o">)</span><span class="w"> </span><span class="o">(</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="kr">rate</span><span class="o">(</span><span class="nv">controller_runtime_reconcile_time_seconds_bucket</span><span class="p">{</span><span class="nl">controller</span><span class="o">=</span><span class="p">&#34;</span><span class="s">stageset</span><span class="p">&#34;}[</span><span class="s">28d</span><span class="p">]</span><span class="o">)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="o">))</span><span class="w">
</span></span></span></code></pre></div><h2 id="see-the-slos-on-the-dashboard">See the SLOs on the dashboard</h2>
<p>The published <a href="/observability/dashboard/">dashboard</a>
 opens with an SLO band:
current availability against its objective, error budget remaining, p95 latency
against its objective, and an availability-versus-objective trend. The
controller-internals panels below explain any movement.</p>
<p>The objectives and window are top-level arguments, so you set them per environment
when you render the dashboard through a JaaS <code>JsonnetSnippet</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">tlas</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">datasource</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;prometheus&#34;</span><span class="p">]</span><span class="w">   </span><span class="c"># your Prometheus datasource UID</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">window</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;28d&#34;</span><span class="p">]</span><span class="w">              </span><span class="c"># SLO window</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">availabilityTarget</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;0.99&#34;</span><span class="p">]</span><span class="w"> </span><span class="c"># 99%</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">latencyTarget</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">&#34;30&#34;</span><span class="p">]</span><span class="w">        </span><span class="c"># seconds</span><span class="w">
</span></span></span></code></pre></div><p>A short window is fine for a demo; a real <code>28d</code> SLI needs at least that much
Prometheus retention. For long windows, precompute the SLI with a recording rule
and point <code>window</code> at the recorded series instead of a raw <code>rate(...[28d])</code>.</p>
<h2 id="alert-on-the-budget">Alert on the budget</h2>
<p>The shipped <a href="/observability/alerting/">alerts</a>
 already page on the <em>causes</em> of SLO
loss (reconcile errors, latency, workqueue saturation). To alert on the objective
itself, add an availability-SLO rule through the chart&rsquo;s <code>extraRules</code> passthrough —
here it fires when recent availability drops below 99%:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">prometheusRule</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">extraRules</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span>- <span class="nt">alert</span><span class="p">:</span><span class="w"> </span><span class="l">StageSetReconcileAvailabilityBelowSLO</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">expr</span><span class="p">:</span><span class="w"> </span><span class="p">|</span><span class="sd">
</span></span></span><span class="line"><span class="cl"><span class="sd">          (
</span></span></span><span class="line"><span class="cl"><span class="sd">            sum(rate(stageset_reconcile_total{reason=&#34;Succeeded&#34;}[1h]))
</span></span></span><span class="line"><span class="cl"><span class="sd">            /
</span></span></span><span class="line"><span class="cl"><span class="sd">            (
</span></span></span><span class="line"><span class="cl"><span class="sd">              sum(rate(stageset_reconcile_total{reason=&#34;Succeeded&#34;}[1h]))
</span></span></span><span class="line"><span class="cl"><span class="sd">              + sum(rate(stageset_reconcile_total{reason!~&#34;Succeeded|Suspended|UpdateDeferred|SourceNotReady|DependencyNotReady&#34;}[1h]))
</span></span></span><span class="line"><span class="cl"><span class="sd">            )
</span></span></span><span class="line"><span class="cl"><span class="sd">          ) &lt; 0.99</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">for</span><span class="p">:</span><span class="w"> </span><span class="l">1h</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">severity</span><span class="p">:</span><span class="w"> </span><span class="l">warning</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">annotations</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">          </span><span class="nt">summary</span><span class="p">:</span><span class="w"> </span><span class="l">StageSet reconcile availability is below its 99% objective</span><span class="w">
</span></span></span></code></pre></div><p>The alert measures a short recent window (<code>1h</code>) so it pages while the budget is
actively burning; the dashboard&rsquo;s <code>window</code> shows the full SLO window. See
<a href="/observability/alerting/">Alerting</a>
 for the rest of the catalog.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/controller" term="controller" label="controller"/><category scheme="https://stageset.projects.metio.wtf/tags/slo" term="slo" label="slo"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/></entry><entry><title type="html">Workqueue saturation</title><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/runbooks/promotionblocked/?utm_source=atom_feed" rel="related" type="text/html" title="PromotionBlocked"/><id>https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>The controller cannot drain its reconcile queue fast enough.</blockquote><h2 id="symptom">Symptom</h2>
<p><code>workqueue_depth{controller=&quot;stageset&quot;}</code> stays high; StageSets reconcile slowly or
lag behind their <code>spec.interval</code>. The <code>StageSetControllerWorkqueueDepthHigh</code> alert
fires (see <a href="/running/operations/">operations</a>
 for the alert set and its
thresholds).</p>
<h2 id="cause">Cause</h2>
<p>The controller is enqueuing reconcile requests faster than it completes them.
Common causes:</p>
<ul>
<li><strong>apiserver slowness</strong> — applies, dry-runs, and status writes all block on the
apiserver (or the impersonated tenant&rsquo;s authorization).</li>
<li><strong>slow sources</strong> — a stage waiting on a large artifact fetch or a source that is
slow to become Ready holds a worker.</li>
<li><strong>a stuck stage</strong> — an action with a long timeout (a <code>wait</code>/<code>http</code>/<code>job</code> that
never completes) pins a worker for the whole timeout.</li>
<li><strong>too few workers for the StageSet count</strong> — many StageSets reconciling on short
intervals.</li>
</ul>
<h2 id="diagnosis">Diagnosis</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl"><span class="c1"># which StageSets are churning?</span>
</span></span><span class="line"><span class="cl">kubectl get stagesets --all-namespaces --sort-by<span class="o">=</span>.status.observedGeneration
</span></span><span class="line"><span class="cl"><span class="c1"># controller logs for slow operations / retries</span>
</span></span><span class="line"><span class="cl">kubectl --namespace stageset-system logs deploy/stageset-controller --tail<span class="o">=</span><span class="m">200</span>
</span></span></code></pre></div><p>Correlate with <code>controller_runtime_reconcile_time_seconds</code> (see
<a href="/runbooks/reconcile-latency/">reconcile latency</a>
) and apiserver latency.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Lengthen <code>spec.interval</code> on high-churn StageSets that don&rsquo;t need fast
reconciliation.</li>
<li>Cap long-running actions with a tighter <code>timeout</code> so a stuck action releases its
worker.</li>
<li>Adding replicas does <strong>not</strong> help: leader election means only the lease holder
reconciles, so a second replica is failover, not added throughput
(<a href="/running/production/#high-availability">production</a>
). The controller has no
reconcile-concurrency flag — the levers are reducing load (longer intervals, fewer
StageSets, fewer objects per stage) and removing the slow operations below.</li>
<li>Investigate apiserver / tenant-authorization latency if reconciles are uniformly
slow.</li>
</ul>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/troubleshooting" term="troubleshooting" label="troubleshooting"/></entry></feed>