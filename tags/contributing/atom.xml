<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Contributing on StageSet Controller</title><link href="https://stageset.projects.metio.wtf/tags/contributing/" rel="alternate" type="text/html" title="html"/><link href="https://stageset.projects.metio.wtf/tags/contributing/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://stageset.projects.metio.wtf/tags/contributing/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-23T07:22:19+00:00</updated><author><name>metio.wtf</name></author><id>https://stageset.projects.metio.wtf/tags/contributing/</id><entry><title type="html">Building and testing</title><link href="https://stageset.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="related" type="text/html" title="CI and releases"/><link href="https://stageset.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="related" type="text/html" title="Claude Code skill"/><link href="https://stageset.projects.metio.wtf/cli/diff/?utm_source=atom_feed" rel="related" type="text/html" title="stagesetctl diff"/><id>https://stageset.projects.metio.wtf/contributing/building/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-07-16T23:05:30+02:00</updated><content type="html"><![CDATA[<blockquote>Build the controller, run the test suite, and pass the static-analysis gate.</blockquote><p>The host needs no Go toolchain. Every command runs through the development shell
that <code>flake.nix</code> defines, with <code>flake.lock</code> pinning each tool to an exact
version:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go build ./...
</span></span><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -race -cover ./...
</span></span></code></pre></div><p>CI runs the same shell, so a gate that is green locally is green there by
construction. Run <code>nix develop</code> on its own to drop into an interactive shell and
call the tools bare.</p>
<h2 id="test-layers">Test layers</h2>
<ul>
<li>
<p><strong>Unit tests</strong> sit next to the code across <code>internal/...</code> and <code>api/v1/</code>. Several
are drift gates — e.g. <code>conditions_test.go</code> asserts every Ready <code>Reason</code> has a
matching runbook page under <code>docs/content/runbooks/</code>.</p>
</li>
<li>
<p><strong>envtest-backed tests</strong> (<code>envtest_*_test.go</code>) boot a real kube-apiserver + etcd
via controller-runtime&rsquo;s <code>envtest</code>. The shell exports <code>KUBEBUILDER_ASSETS</code>
pointing at an <code>etcd</code> + <code>kube-apiserver</code> + <code>kubectl</code> bundle assembled from
nixpkgs, so they run offline with nothing to install; they <code>t.Skip</code> when it is
unset.</p>
</li>
<li>
<p><strong>Fuzz tests</strong> (<code>FuzzXxx</code>) harden the parsing-heavy paths; their seed corpus runs
as ordinary unit tests, and <code>-fuzz</code> fuzzes for real:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go <span class="nb">test</span> -run<span class="o">=</span>^$ -fuzz<span class="o">=</span>^FuzzName$ -fuzztime<span class="o">=</span>30s ./internal/&lt;pkg&gt;/
</span></span></code></pre></div></li>
<li>
<p><strong>Kind smoke</strong> scenarios under <code>hack/smoke/</code> run the controller end to end
against a real kind cluster.</p>
</li>
</ul>
<h2 id="regenerating-generated-code">Regenerating generated code</h2>
<p>The CRDs under <code>config/crd/</code>, <code>config/rbac/role.yaml</code> (rendered from the
<code>+kubebuilder:rbac</code> markers), <code>config/webhook/manifests.yaml</code>, and
<code>api/v1/zz_generated.deepcopy.go</code> are produced by <code>controller-gen</code>. Regenerate
them after touching <code>api/</code> or a marker, and commit the result:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command generate
</span></span></code></pre></div><p><code>verify.yml</code>&rsquo;s <code>generated</code> job runs that same command and fails on any diff, so
stale manifests cannot ship.</p>
<h2 id="building-the-site">Building the site</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command website   <span class="c1"># one-shot build into docs/public/</span>
</span></span><span class="line"><span class="cl">nix develop --command serve     <span class="c1"># live server on :1313</span>
</span></span><span class="line"><span class="cl">nix develop --command htmltest  <span class="c1"># lint the rendered HTML (needs a build first)</span>
</span></span></code></pre></div><h2 id="static-analysis">Static analysis</h2>
<p>A pull request must be clean under each of these — run them locally before
pushing:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">nix develop --command go vet ./...
</span></span><span class="line"><span class="cl">nix develop --command staticcheck ./...      <span class="c1"># config: staticcheck.conf, checks = [&#34;all&#34;]</span>
</span></span><span class="line"><span class="cl">nix develop --command gosec ./...
</span></span><span class="line"><span class="cl">nix develop --command govulncheck ./...
</span></span><span class="line"><span class="cl">nix develop --command gofumpt -l .           <span class="c1"># empty output == formatted</span>
</span></span><span class="line"><span class="cl">nix develop --command arch-go                <span class="c1"># architecture rules (arch-go.yml)</span>
</span></span><span class="line"><span class="cl">nix develop --command modernize ./...        <span class="c1"># newer-Go idiom check</span>
</span></span></code></pre></div>]]></content><category scheme="https://stageset.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://stageset.projects.metio.wtf/tags/ci" term="ci" label="ci"/></entry><entry><title type="html">CI and releases</title><link href="https://stageset.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="related" type="text/html" title="Building and testing"/><link href="https://stageset.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="related" type="text/html" title="Claude Code skill"/><link href="https://stageset.projects.metio.wtf/cli/diff/?utm_source=atom_feed" rel="related" type="text/html" title="stagesetctl diff"/><id>https://stageset.projects.metio.wtf/contributing/ci-and-release/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-18T20:07:19+02:00</updated><content type="html"><![CDATA[<blockquote>The pull-request gates and the automated calendar-based release process.</blockquote><h2 id="continuous-integration">Continuous integration</h2>
<p>Every pull request runs <code>verify.yml</code>, which fans out into one job per concern so a
failure points straight at the cause:</p>
<ul>
<li><strong>test</strong> — <code>go build</code> then the full <code>go test</code> suite.</li>
<li><strong>lint-go</strong> — <code>go vet</code>, <code>staticcheck</code>, <code>gosec</code>, and a <code>gofumpt</code> formatting check.</li>
<li><strong>vulnerabilities</strong> — <code>govulncheck</code> (a reachable advisory is a hard gate).</li>
<li><strong>architecture</strong> — <code>arch-go</code> against <code>arch-go.yml</code>.</li>
<li><strong>reuse</strong> — SPDX/REUSE compliance on every file.</li>
<li><strong>text linters</strong> — <code>yamllint</code>, <code>actionlint</code>, <code>markdownlint</code>, <code>typos</code>, and Vale
prose linting.</li>
<li><strong>docs-lint</strong> — builds the Hugo site and lints the rendered output.</li>
<li><strong>container-image</strong> — a buildx image build plus a Trivy scan.</li>
<li><strong>dco</strong> — every non-bot commit carries a <code>Signed-off-by</code> trailer.</li>
</ul>
<p>A single <strong>all-green</strong> job depends on every other job and is the only required
check, so new jobs are covered automatically. A separate <code>kind-smoke.yml</code> runs the
operator end to end against a real kind cluster, and <code>fuzz.yml</code> exercises the fuzz
targets.</p>
<h2 id="releases">Releases</h2>
<p>Releases are <strong>calendar-based and fully automated</strong> — there is no semver tag to
bump by hand. <code>release.yml</code> runs on a Monday cron (and on manual dispatch), and the
version is the run date (<code>date +'%Y.%-m.%-d'</code>, e.g. <code>2026.6.15</code>). A prepare job
counts commits since the last release; an empty week publishes nothing.</p>
<p>The pipeline is hand-rolled — no goreleaser, no GPG:</p>
<ul>
<li>Binaries are cross-compiled with <code>go build</code> (<code>CGO_ENABLED=0</code>, <code>-trimpath</code>,
<code>-ldflags</code>) and archived per platform.</li>
<li>A multi-arch image is pushed to <code>ghcr.io/metio/stageset-controller</code> and signed
with <strong>cosign keyless</strong> (Fulcio OIDC) by digest.</li>
<li>The GitHub Release attaches the archives, a <code>SHA256SUMS</code> file, and its cosign
signature; identity is proven by the workflow&rsquo;s OIDC certificate, so there is no
key to distribute.</li>
</ul>
<p>The Helm chart lives in the <a href="https://github.com/metio/helm-charts">metio/helm-charts</a>

repository and vendors this repo&rsquo;s CRDs at each release; its <code>appVersion</code> tracks the
binary&rsquo;s releases.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://stageset.projects.metio.wtf/tags/ci" term="ci" label="ci"/><category scheme="https://stageset.projects.metio.wtf/tags/release" term="release" label="release"/></entry><entry><title type="html">Claude Code skill</title><link href="https://stageset.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="related" type="text/html" title="Building and testing"/><link href="https://stageset.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="related" type="text/html" title="CI and releases"/><id>https://stageset.projects.metio.wtf/integrations/claude-code/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Install the StageSet plugin so Claude Code authors and operates StageSets.</blockquote><p>The repository ships a <a href="https://claude.com/claude-code">Claude Code</a>
 skill that
teaches the assistant to author and operate <code>StageSet</code> resources: writing and
editing StageSet YAML, wiring a Flux source or a producer like
<a href="https://jaas.projects.metio.wtf/">JaaS</a>
 into staged rollouts, adding typed
actions, ready checks, update windows, versioned migrations, and conflict
policies, and driving a StageSet with the <code>stagesetctl</code> CLI. With it installed,
Claude Code works against the real schema and defaults instead of guessing.</p>
<h2 id="install-the-plugin">Install the plugin</h2>
<p>The skill is packaged as a plugin in a marketplace. Add the marketplace from the
repository, then install the plugin:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">/plugin marketplace add metio/stageset-controller
</span></span><span class="line"><span class="cl">/plugin install stageset@stageset
</span></span></code></pre></div><p>The first command registers the <code>stageset</code> marketplace from this repository; the
second installs the <code>stageset</code> plugin from it. Restart Claude Code if prompted, and
the skill activates automatically whenever a repository contains StageSet manifests
or the stageset-controller is in play.</p>
<h2 id="what-the-skill-knows">What the skill knows</h2>
<ul>
<li><strong>Authoring.</strong> It starts from a minimal StageSet (only <code>spec.stages</code> is
required) and layers on options in order of need — more stages,
<code>serviceAccountName</code> impersonation, <code>decryption</code>, per-stage build surface,
actions, ready checks, conflict policies, update windows, versioned migrations,
and rollback.</li>
<li><strong>Source wiring.</strong> It knows <code>sourceRef.kind</code> defaults to <code>ExternalArtifact</code>, that
a stage can point directly at a <code>GitRepository</code> / <code>OCIRepository</code> / <code>Bucket</code>, and
when to use a producer such as a JaaS <code>JsonnetSnippet</code> instead.</li>
<li><strong>Operating.</strong> It drives <code>stagesetctl</code> (also usable as <code>kubectl stageset</code>) to
preview and reconcile — <code>diff</code>, <code>build</code>, <code>get</code>, <code>reconcile</code> — and previews
changes before applying them.</li>
<li><strong>Debugging.</strong> It maps <code>status.conditions[Ready].reason</code> to the matching
<a href="/runbooks/">runbook</a>
 and reads <code>status.stages[]</code> for per-stage phase, applied
revision, and executed actions.</li>
</ul>
<p>The skill treats this site as its source of truth, fetching exact fields and
defaults from the <a href="/api/stageset/">API reference</a>
, the <a href="/defining-a-release/">usage</a>
 pages, and
the <a href="/cli/">CLI</a>
 reference rather than relying on memory.</p>
<h2 id="where-it-lives">Where it lives</h2>
<p>The skill source is under <code>skills/stageset/</code> — <code>SKILL.md</code> plus a
<code>references/reference.md</code> cheat-sheet — and the plugin and marketplace manifests
are under <code>.claude-plugin/</code>. Edit those files to change what the assistant knows;
the install commands above pull whatever the repository currently ships.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://stageset.projects.metio.wtf/tags/claude-code" term="claude-code" label="claude-code"/><category scheme="https://stageset.projects.metio.wtf/tags/skill" term="skill" label="skill"/><category scheme="https://stageset.projects.metio.wtf/tags/plugin" term="plugin" label="plugin"/></entry><entry><title type="html">Contributing</title><link href="https://stageset.projects.metio.wtf/contributing/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/contributing/building/?utm_source=atom_feed" rel="related" type="text/html" title="Building and testing"/><link href="https://stageset.projects.metio.wtf/contributing/ci-and-release/?utm_source=atom_feed" rel="related" type="text/html" title="CI and releases"/><link href="https://stageset.projects.metio.wtf/integrations/claude-code/?utm_source=atom_feed" rel="related" type="text/html" title="Claude Code skill"/><id>https://stageset.projects.metio.wtf/contributing/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Build and test stageset-controller locally, pass the pull-request checks, and understand how releases are cut.</blockquote><p>Contributions are welcome. Build and test the controller locally, satisfy the
checks every pull request must pass, and see how releases are cut.</p>
<h2 id="developer-certificate-of-origin">Developer Certificate of Origin</h2>
<p>Every commit must be signed off under the
<a href="https://developercertificate.org/">Developer Certificate of Origin</a>
 — it
certifies you wrote the patch or otherwise have the right to contribute it. Add
the sign-off automatically with:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">git commit --signoff
</span></span></code></pre></div><p>This appends a <code>Signed-off-by: Your Name &lt;you@example.com&gt;</code> line to the commit
message. The DCO check on each pull request enforces it; unsigned commits block
the merge. Amend an existing commit with <code>git commit --amend --signoff</code>.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/contributing" term="contributing" label="contributing"/><category scheme="https://stageset.projects.metio.wtf/tags/development" term="development" label="development"/><category scheme="https://stageset.projects.metio.wtf/tags/dco" term="dco" label="dco"/></entry></feed>