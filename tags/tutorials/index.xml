<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Tutorials on StageSet Controller</title><link>https://stageset.projects.metio.wtf/tags/tutorials/</link><description>Recent content in Tutorials on StageSet Controller</description><generator>Hugo</generator><language>en</language><atom:link href="https://stageset.projects.metio.wtf/tags/tutorials/index.xml" rel="self" type="application/rss+xml"/><item><title>From Jsonnet to a gated rollout</title><link>https://stageset.projects.metio.wtf/get-started/jsonnet-to-rollout/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/get-started/jsonnet-to-rollout/</guid><description>&lt;p&gt;This tutorial follows a complete delivery: write &lt;a href="https://kubernetes.io/docs/"&gt;Kubernetes&lt;/a&gt;

manifests in &lt;a href="https://jsonnet.org/"&gt;Jsonnet&lt;/a&gt;
 and publish the source through
&lt;a href="https://fluxcd.io/"&gt;Flux&lt;/a&gt;
; &lt;a href="https://jaas.projects.metio.wtf/"&gt;JaaS&lt;/a&gt;
 renders it into a
Flux &lt;code&gt;ExternalArtifact&lt;/code&gt;, and a StageSet rolls it out with a readiness gate.&lt;/p&gt;
&lt;p&gt;The chain is:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Jsonnet in Git/OCI/Bucket → JaaS (JsonnetSnippet) → ExternalArtifact → StageSet
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This tutorial renders &lt;em&gt;Jsonnet&lt;/em&gt;, so it goes through JaaS: JaaS turns the Jsonnet
into an &lt;code&gt;ExternalArtifact&lt;/code&gt; the stage consumes. (If your manifests were already plain
YAML, a stage could read a &lt;code&gt;GitRepository&lt;/code&gt;/&lt;code&gt;OCIRepository&lt;/code&gt;/&lt;code&gt;Bucket&lt;/code&gt; directly — see
&lt;a href="https://stageset.projects.metio.wtf/get-started/flux-sources/"&gt;Stage sources&lt;/a&gt;
. The renderer is here because the input is
Jsonnet, not because StageSet can&amp;rsquo;t read Git.)&lt;/p&gt;</description></item><item><title>Generating migrations with Jsonnet</title><link>https://stageset.projects.metio.wtf/guides/jsonnet-migrations/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/guides/jsonnet-migrations/</guid><description>&lt;p&gt;A &lt;a href="https://stageset.projects.metio.wtf/gating/versioned-migrations/"&gt;migration ladder&lt;/a&gt;
 is just data — a list of
version-gated actions. So you can &lt;em&gt;generate&lt;/em&gt; it with &lt;a href="https://jsonnet.org/"&gt;Jsonnet&lt;/a&gt;
,
publish it once as a Flux artifact with &lt;a href="https://jaas.projects.metio.wtf/"&gt;JaaS&lt;/a&gt;
,
and have every StageSet that deploys the app consume it through
&lt;code&gt;spec.migrationsSourceRef&lt;/code&gt;. One ladder, authored once, shared across namespaces.&lt;/p&gt;
&lt;p&gt;The chain mirrors &lt;a href="https://stageset.projects.metio.wtf/get-started/jsonnet-to-rollout/"&gt;From Jsonnet to a gated rollout&lt;/a&gt;
,
but the rendered artifact is the &lt;em&gt;migrations&lt;/em&gt; rather than the manifests:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;Jsonnet ladder → JaaS (JsonnetSnippet) → ExternalArtifact → StageSet.migrationsSourceRef
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h2 id="prerequisites"&gt;Prerequisites&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Flux installed (with the &lt;code&gt;ExternalArtifact&lt;/code&gt; API — Flux ≥ v2.7.0).&lt;/li&gt;
&lt;li&gt;&lt;a href="https://jaas.projects.metio.wtf/"&gt;JaaS&lt;/a&gt;
 installed in operator mode.&lt;/li&gt;
&lt;li&gt;StageSet installed (see &lt;a href="https://stageset.projects.metio.wtf/get-started/kubernetes/"&gt;Installation&lt;/a&gt;
).&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="1-write-the-ladder-in-jsonnet"&gt;1. Write the ladder in Jsonnet&lt;/h2&gt;
&lt;p&gt;The snippet evaluates to a &lt;strong&gt;JSON array of migrations&lt;/strong&gt; — the same shape the
ladder file uses, so JaaS&amp;rsquo;s &lt;code&gt;rendered.json&lt;/code&gt; &lt;em&gt;is&lt;/em&gt; the ladder. A small helper keeps
each entry readable, and the file is trivially parameterizable (per app, per
environment) with top-level args:&lt;/p&gt;</description></item><item><title>Guides</title><link>https://stageset.projects.metio.wtf/guides/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/guides/</guid><description>&lt;p&gt;Task-focused walkthroughs that go deeper than &lt;a href="https://stageset.projects.metio.wtf/get-started/"&gt;Get started&lt;/a&gt;
. Each
follows a whole job from start to finish; for a single feature in isolation, see
the feature pages under &lt;a href="https://stageset.projects.metio.wtf/defining-a-release/"&gt;Defining a release&lt;/a&gt;
 and
&lt;a href="https://stageset.projects.metio.wtf/gating/"&gt;Gating &amp;amp; rollout safety&lt;/a&gt;
.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://stageset.projects.metio.wtf/guides/parameters/"&gt;Parameterizing a rollout&lt;/a&gt;
&lt;/strong&gt; — render-time and
delivery-time parameters.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://stageset.projects.metio.wtf/guides/progressive-delivery/"&gt;Progressive delivery&lt;/a&gt;
&lt;/strong&gt; — hold a promotion
behind Flagger or an Argo Rollouts metric gate.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://stageset.projects.metio.wtf/guides/jsonnet-migrations/"&gt;Generating migrations with Jsonnet&lt;/a&gt;
&lt;/strong&gt; — build a
shared migration ladder.
&lt;/content&gt;&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>Parameterizing a rollout</title><link>https://stageset.projects.metio.wtf/guides/parameters/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/guides/parameters/</guid><description>&lt;p&gt;A rollout takes parameters at two distinct layers, which serve different purposes:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Render-time parameters (JaaS).&lt;/strong&gt; Change &lt;em&gt;what gets rendered&lt;/em&gt;. The Jsonnet
computes its output from top-level arguments (&lt;code&gt;tlas&lt;/code&gt;) and external variables
(&lt;code&gt;externalVariables&lt;/code&gt;). Different values produce a different &lt;code&gt;ExternalArtifact&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Delivery-time parameters (StageSet &lt;code&gt;postBuild&lt;/code&gt;).&lt;/strong&gt; Inject values &lt;em&gt;into
already-rendered manifests&lt;/em&gt;, per stage, by string substitution — the same
mechanism Flux&amp;rsquo;s &lt;code&gt;kustomize-controller&lt;/code&gt; uses.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Use render-time parameters for structural logic; use delivery-time parameters to
stamp environment-specific values onto a shared artifact.&lt;/p&gt;</description></item><item><title>Progressive delivery</title><link>https://stageset.projects.metio.wtf/guides/progressive-delivery/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/guides/progressive-delivery/</guid><description>&lt;p&gt;&lt;code&gt;StageSet&lt;/code&gt; integrates with both progressive-delivery controllers:
&lt;a href="https://flagger.app/"&gt;Flagger&lt;/a&gt;
 and
&lt;a href="https://argoproj.github.io/argo-rollouts/"&gt;Argo Rollouts&lt;/a&gt;
. The controller exposes
a read-only gate endpoint and a readiness gauge so either one can hold a promotion
until a &lt;code&gt;StageSet&lt;/code&gt; stage is healthy; ready checks let a stage wait on a Rollout in
return. See also &lt;a href="https://stageset.projects.metio.wtf/comparisons/argo-rollouts/"&gt;StageSet vs Argo Rollouts&lt;/a&gt;
.&lt;/p&gt;
&lt;h2 id="the-gate-contract"&gt;The gate contract&lt;/h2&gt;
&lt;p&gt;The gate endpoint backs the Flagger integration and the Argo Rollouts JSON-metric
option.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;GET /gate/{namespace}/{stageset}/{stage}
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; 200 — the stage is Ready at the currently pinned revision
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; 403 — the stage is not Ready (or not found / not gateable)
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;It is served on &lt;code&gt;--gate-bind-address&lt;/code&gt; (default &lt;code&gt;:8082&lt;/code&gt;) and exposed by the chart&amp;rsquo;s
&lt;code&gt;stageset-controller-gate&lt;/code&gt; Service (&lt;code&gt;gate.enabled&lt;/code&gt;, on by default). The endpoint is
&lt;strong&gt;unauthenticated and read-only&lt;/strong&gt;, so fence it with a &lt;code&gt;NetworkPolicy&lt;/code&gt;
(&lt;a href="https://stageset.projects.metio.wtf/running/production/#network-policy"&gt;production&lt;/a&gt;
) to admit only your
delivery controller.&lt;/p&gt;</description></item><item><title>Quickstart</title><link>https://stageset.projects.metio.wtf/get-started/quickstart/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/get-started/quickstart/</guid><description>&lt;p&gt;This tutorial takes you from an empty cluster to one running StageSet. The path
is the shortest one — a single stage pointing directly at a Flux
&lt;code&gt;GitRepository&lt;/code&gt; that already holds plain manifests. No Jsonnet, no migrations,
no optional knobs.&lt;/p&gt;
&lt;h2 id="prerequisites"&gt;Prerequisites&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;A &lt;a href="https://kubernetes.io/docs/"&gt;Kubernetes&lt;/a&gt;
 cluster with &lt;code&gt;kubectl&lt;/code&gt; configured
against it.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;helm&lt;/code&gt; 3.x.&lt;/li&gt;
&lt;li&gt;&lt;a href="https://fluxcd.io/"&gt;Flux&lt;/a&gt;
 &lt;strong&gt;v2.7.0 or newer&lt;/strong&gt; — the &lt;code&gt;ExternalArtifact&lt;/code&gt; CRD a
stage resolves to lands in that version. See
&lt;a href="https://stageset.projects.metio.wtf/get-started/kubernetes/#prerequisites"&gt;Install on Kubernetes&lt;/a&gt;
 for the full
prerequisites.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="step-1--install-the-controller"&gt;Step 1 — Install the controller&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-shell" data-lang="shell"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;helm upgrade --install stageset-controller &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; oci://ghcr.io/metio/helm-charts/stageset-controller &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --namespace stageset-system --create-namespace &lt;span class="se"&gt;\
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; --wait --timeout 5m
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;See &lt;a href="https://stageset.projects.metio.wtf/get-started/kubernetes/"&gt;Install on Kubernetes&lt;/a&gt;
 for the full list of chart
values (HA replicas, rollback store, webhook TLS mode, and so on).&lt;/p&gt;</description></item><item><title>Stage sources — Git, OCI, Bucket</title><link>https://stageset.projects.metio.wtf/get-started/flux-sources/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://stageset.projects.metio.wtf/get-started/flux-sources/</guid><description>&lt;p&gt;A stage resolves its &lt;code&gt;sourceRef&lt;/code&gt; to a &lt;a href="https://fluxcd.io/"&gt;Flux&lt;/a&gt;
 artifact. You have
two routes:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;manifests in Git / OCI / Bucket ──────────────────────────► StageSet (direct)
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;manifests in Git / OCI / Bucket ──► a renderer (JaaS) ──► ExternalArtifact ──► StageSet
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Use the &lt;strong&gt;direct&lt;/strong&gt; route when the source already holds ready-to-apply manifests
(the same thing Flux&amp;rsquo;s &lt;code&gt;kustomize-controller&lt;/code&gt; consumes). Use the &lt;strong&gt;renderer&lt;/strong&gt; route
when you generate manifests first — e.g. evaluating Jsonnet with
&lt;a href="https://jaas.projects.metio.wtf/"&gt;JaaS&lt;/a&gt;
.&lt;/p&gt;
&lt;p&gt;Copy-pasteable recipes follow, one per source kind. For how &lt;code&gt;sourceRef&lt;/code&gt;
resolution works as a concept — and the &lt;code&gt;path&lt;/code&gt;, &lt;code&gt;prune&lt;/code&gt;, &lt;code&gt;patches&lt;/code&gt;, and
&lt;code&gt;postBuild&lt;/code&gt; knobs that shape a stage — see
&lt;a href="https://stageset.projects.metio.wtf/defining-a-release/stages-and-sources/"&gt;stages and sources&lt;/a&gt;
.&lt;/p&gt;</description></item></channel></rss>