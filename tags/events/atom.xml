<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Events on StageSet Controller</title><link href="https://stageset.projects.metio.wtf/tags/events/" rel="alternate" type="text/html" title="html"/><link href="https://stageset.projects.metio.wtf/tags/events/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://stageset.projects.metio.wtf/tags/events/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-23T07:22:19+00:00</updated><author><name>metio.wtf</name></author><id>https://stageset.projects.metio.wtf/tags/events/</id><entry><title type="html">Alerting</title><link href="https://stageset.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/observability/metrics/?utm_source=atom_feed" rel="related" type="text/html" title="Metrics"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><id>https://stageset.projects.metio.wtf/observability/alerting/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-19T09:16:22+02:00</updated><content type="html"><![CDATA[<blockquote>The controller&rsquo;s Kubernetes Events on StageSet transitions, the opt-in PrometheusRule alert catalog with tunable thresholds, and how every alert and Ready reason links to a runbook.</blockquote><p>Two layers surface trouble: Kubernetes Events the controller emits on a StageSet&rsquo;s
own timeline, and a Prometheus alert set built on the <a href="/observability/metrics/">metrics</a>
.
Both lead an operator to a <a href="/runbooks/">runbook</a>
.</p>
<p>These alerts page on the <em>causes</em> of trouble. To alert on a
<a href="/observability/slos/">service-level objective</a>
 directly, and to see availability,
error budget, and latency against their objectives, use the published
<a href="/observability/dashboard/">dashboard</a>
.</p>
<h2 id="the-controller-binary">The controller binary</h2>
<h3 id="kubernetes-events">Kubernetes Events</h3>
<p>On a StageSet&rsquo;s condition transitions and notable actions, the controller emits
standard Kubernetes Events (events.v1) against the StageSet object. The reason
fills both the Event <code>reason</code> and <code>action</code> slots. Read them with:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl --namespace team-a describe stageset checkout
</span></span><span class="line"><span class="cl"><span class="c1"># or, just the events:</span>
</span></span><span class="line"><span class="cl">kubectl --namespace team-a get events --field-selector involvedObject.name<span class="o">=</span>checkout
</span></span></code></pre></div><p>Reasons you will see include <code>Succeeded</code> and <code>StageFailed</code> on the rollout outcome,
<code>UpdateDeferred</code> when an update window holds a rollout, <code>MigrationStarted</code> /
<code>MigrationCompleted</code> / <code>MigrationFailed</code> around a versioned migration,
<code>DriftCorrected</code> when an apply repaired out-of-band drift, <code>RolledBack</code> when
<code>rollbackOnFailure</code> restored the last-good revisions, and <code>RollbackStoreFailed</code> /
<code>OnFailureAction</code> for best-effort side paths. Because Events target a <code>StageSet</code>,
Flux&rsquo;s <code>notification-controller</code> can route them to Slack, Teams, or any provider by
declaring an <code>Alert</code> that targets <code>kind: StageSet</code> — no extra plumbing on the
controller side.</p>
<h3 id="ready-reasons-link-to-runbooks">Ready reasons link to runbooks</h3>
<p>Every actionable Ready-condition reason maps to a runbook page at
<code>/runbooks/&lt;reason&gt;/</code>. The controller threads a
<code>(runbook: https://stageset.projects.metio.wtf/runbooks/&lt;reason&gt;/)</code> suffix onto
actionable Ready-condition messages, so <code>kubectl describe stageset</code> links straight
to the remediation page. Intentional, steady states — <code>Succeeded</code>, <code>Suspended</code> —
carry no suffix, since there is nothing to remediate.</p>
<h2 id="the-helm-chart">The Helm chart</h2>
<p>The chart ships an opt-in <code>PrometheusRule</code> named <code>&lt;release&gt;-alerts</code> with a starter
alert set. It requires the Prometheus operator CRDs. Enable it and route every
alert to one Alertmanager receiver with <code>extraAlertLabels</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">prometheusRule</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l">30s</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># Labels Prometheus selects the rule object on.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">release</span><span class="p">:</span><span class="w"> </span><span class="l">kube-prometheus-stack</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># Merged onto every rendered alert, so all stageset alerts route together.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">extraAlertLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">team</span><span class="p">:</span><span class="w"> </span><span class="l">platform</span><span class="w">
</span></span></span></code></pre></div><h3 id="shipped-alerts">Shipped alerts</h3>
<table>
	<thead>
			<tr>
					<th>Alert</th>
					<th>Severity</th>
					<th>Fires when</th>
					<th>Threshold knobs</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>StageSetReconcileErrorsHigh</code></td>
					<td>warning</td>
					<td>Per-StageSet Ready=False rate (5m window) exceeds the rate, excluding the happy <code>Succeeded</code> and <code>Suspended</code> reasons.</td>
					<td><code>reconcileErrorRate</code> (<code>0.1</code>/s), <code>reconcileErrorDuration</code> (<code>10m</code>)</td>
			</tr>
			<tr>
					<td><code>StageSetControllerWorkqueueDepthHigh</code></td>
					<td>warning</td>
					<td><code>workqueue_depth{controller=&quot;stageset&quot;}</code> exceeds the depth.</td>
					<td><code>workqueueDepth</code> (<code>50</code>), <code>workqueueDuration</code> (<code>15m</code>)</td>
			</tr>
			<tr>
					<td><code>StageSetReconcileLatencyHigh</code></td>
					<td>warning</td>
					<td>Reconcile p99 (10m window) exceeds the ceiling in seconds.</td>
					<td><code>reconcileLatencySeconds</code> (<code>30</code>), <code>reconcileLatencyDuration</code> (<code>15m</code>)</td>
			</tr>
			<tr>
					<td><code>StageSetControllerPodDown</code></td>
					<td>critical</td>
					<td>A controller pod is NotReady for the window.</td>
					<td><code>podDownDuration</code> (<code>5m</code>)</td>
			</tr>
			<tr>
					<td><code>StageSetWebhookCertRenewalFailing</code></td>
					<td>critical</td>
					<td><code>stageset_webhook_cert_renewal_failures_total</code> increases over 1h beyond the count.</td>
					<td><code>webhookCertRenewalFailuresPerHour</code> (<code>1</code>), <code>webhookCertRenewalFailuresDuration</code> (<code>30m</code>)</td>
			</tr>
			<tr>
					<td><code>StageSetWatchEngagementFailing</code></td>
					<td>warning</td>
					<td><code>stageset_watch_engagement_failures_total{gvk=...}</code> increases over 1h beyond the count — a producer watch failed to engage, so StageSets referencing that kind stop re-triggering on its upstream changes.</td>
					<td><code>watchEngagementFailuresPerHour</code> (<code>1</code>), <code>watchEngagementFailuresDuration</code> (<code>30m</code>)</td>
			</tr>
	</tbody>
</table>
<p>Each threshold lives under <code>metrics.prometheusRule.thresholds</code>. To silence a
built-in alert without forking the chart, raise its threshold to an impossibly high
value; to add alerts, append them under <code>metrics.prometheusRule.extraRules</code>, which
renders verbatim in a separate <code>stageset-extras</code> group.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">prometheusRule</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">thresholds</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">reconcileErrorRate</span><span class="p">:</span><span class="w"> </span><span class="m">0.1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">reconcileErrorDuration</span><span class="p">:</span><span class="w"> </span><span class="l">10m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">workqueueDepth</span><span class="p">:</span><span class="w"> </span><span class="m">50</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">workqueueDuration</span><span class="p">:</span><span class="w"> </span><span class="l">15m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">reconcileLatencySeconds</span><span class="p">:</span><span class="w"> </span><span class="m">30</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">reconcileLatencyDuration</span><span class="p">:</span><span class="w"> </span><span class="l">15m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">podDownDuration</span><span class="p">:</span><span class="w"> </span><span class="l">5m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">webhookCertRenewalFailuresPerHour</span><span class="p">:</span><span class="w"> </span><span class="m">1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">webhookCertRenewalFailuresDuration</span><span class="p">:</span><span class="w"> </span><span class="l">30m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">watchEngagementFailuresPerHour</span><span class="p">:</span><span class="w"> </span><span class="m">1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">watchEngagementFailuresDuration</span><span class="p">:</span><span class="w"> </span><span class="l">30m</span><span class="w">
</span></span></span></code></pre></div><h3 id="alerts-link-to-runbooks">Alerts link to runbooks</h3>
<p>Each alert carries a runbook link in its <code>runbook_url</code> annotation (the annotation
key is <code>metrics.prometheusRule.runbookAnnotationKey</code>; the URL prefix is fixed to
this site&rsquo;s <a href="/runbooks/">runbooks</a>
). <code>StageSetReconcileErrorsHigh</code> templates its
link on the <code>reason</code> label — every Ready-condition reason maps to a runbook at
<code>/runbooks/&lt;reason&gt;/</code> — while the availability, webhook, and
watch-engagement alerts point at their fixed pages
(<a href="/runbooks/workqueue-saturation/">workqueue-saturation</a>
,
<a href="/runbooks/reconcile-latency/">reconcile-latency</a>
,
<a href="/runbooks/controller-pod-down/">controller-pod-down</a>
,
<a href="/runbooks/webhook-cert-renewal/">webhook-cert-renewal</a>
,
<a href="/runbooks/watch-engagement/">watch-engagement</a>
).</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/observability" term="observability" label="observability"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/prometheus" term="prometheus" label="prometheus"/><category scheme="https://stageset.projects.metio.wtf/tags/events" term="events" label="events"/><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/></entry></feed>