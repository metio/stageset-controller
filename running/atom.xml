<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://gohugo.io/" version="0.163.3">Hugo</generator><title type="html">Run in production on StageSet Controller</title><link href="https://stageset.projects.metio.wtf/running/" rel="alternate" type="text/html" title="html"/><link href="https://stageset.projects.metio.wtf/running/index.xml" rel="alternate" type="application/rss+xml" title="rss"/><link href="https://stageset.projects.metio.wtf/running/atom.xml" rel="self" type="application/atom+xml" title="atom"/><updated>2026-07-01T08:41:18+00:00</updated><author><name>metio.wtf</name></author><id>https://stageset.projects.metio.wtf/running/</id><entry><title type="html">Backup and disaster recovery</title><link href="https://stageset.projects.metio.wtf/running/disaster-recovery/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/defining-a-release/inventory/?utm_source=atom_feed" rel="related" type="text/html" title="Inventory and pruning"/><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="related" type="text/html" title="Operations"/><link href="https://stageset.projects.metio.wtf/running/production/?utm_source=atom_feed" rel="related" type="text/html" title="Production"/><link href="https://stageset.projects.metio.wtf/security/encryption/?utm_source=atom_feed" rel="related" type="text/html" title="Secrets encryption"/><id>https://stageset.projects.metio.wtf/running/disaster-recovery/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Back up the state the controller cannot fully reconstruct, and restore a cluster in the order that keeps pruning, teardown, and decryption safe.</blockquote><p>Back up the small set of state the controller cannot reconstruct, then rebuild a
lost cluster by restoring that state in the right order. Most of what a <code>StageSet</code>
manages is recomputed on the next reconcile from your Git repository and your Flux
sources, so it needs no backup of its own. Three things are the exception, and
losing any of them causes silent data damage rather than a clean failure.</p>
<h2 id="what-the-controller-rebuilds-and-what-it-cannot">What the controller rebuilds, and what it cannot</h2>
<p>A <code>StageSet</code> is a function of its source content: given the same spec and the same
artifacts, a reconcile re-fetches, re-renders, and re-applies the same objects.
The applied workloads, the <code>StageSet</code> status, the webhook&rsquo;s self-signed CA, and the
leader lease are all reconstructed automatically. Three pieces of state need
backup attention — two are unrecoverable if lost, and the third self-heals for the
common case but is still worth backing up:</p>
<ol>
<li><strong><code>StageInventory</code> resources</strong> — the record of exactly which objects each stage
owns, read to prune and to tear down. If it is lost while the stage&rsquo;s objects are
still live, the controller <strong>self-heals</strong>: the next reconcile rebuilds the
inventory from the objects&rsquo; owner and per-stage labels (across the GVKs the
current render touches), emits an <code>InventoryReconstructed</code> event, and <strong>defers
pruning that pass</strong> so it never deletes against a best-effort rebuild; the
following reconcile prunes normally. The residual gap is a kind the current
render no longer contains at all — those objects still carry the labels but
aren&rsquo;t swept, so they stay orphaned. Back up the inventory so that gap, and
reverse-order teardown, stay correct. It lives in <code>spec</code> precisely so a
spec-restoring backup tool captures it.</li>
<li><strong>Encryption key Secrets</strong> — the age or PGP private keys named by
<code>spec.decryption.secretRef</code>. They are unrecoverable if lost. A
<a href="/gating/rollback/">rollback</a>
 that must re-decrypt a prior revision fails closed
with reason <code>PreviousRevisionUnavailable</code>, and any new run that decrypts SOPS
files fails until the keys are restored. Cloud-KMS keys live outside the cluster;
back up the KMS key policy or ARN instead of the key material.</li>
<li><strong>Rollback store contents</strong> — when <code>spec.rollbackOnFailure: true</code> and the
producer retains only one revision, the <a href="/gating/rollback/">rollback store</a>
 (a
RWX <code>PersistentVolume</code> or an S3 bucket) holds the sole copy of the last-good
rendered output. It is encrypted at rest because it contains decrypted Secrets.</li>
</ol>
<h2 id="state-inventory">State inventory</h2>
<table>
	<thead>
			<tr>
					<th>Component</th>
					<th>Source of truth</th>
					<th>Regeneratable?</th>
					<th>Back up?</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>StageSet</code> spec</td>
					<td>your Git repository / Flux source</td>
					<td>yes — re-applied by Flux</td>
					<td>through the GitOps repo</td>
			</tr>
			<tr>
					<td><code>StageSet</code> status</td>
					<td>rebuilt by reconcile</td>
					<td>yes — but <code>status.lastAppliedSnapshot</code> is the rollback target</td>
					<td>via an etcd snapshot</td>
			</tr>
			<tr>
					<td><strong><code>StageInventory</code></strong></td>
					<td>the controller (cluster state only)</td>
					<td><strong>best-effort self-heal from live objects</strong></td>
					<td><strong>recommended (covers the residual-GVK gap + teardown)</strong></td>
			</tr>
			<tr>
					<td><strong>Encryption key Secrets</strong></td>
					<td><code>spec.decryption.secretRef</code></td>
					<td><strong>no — unrecoverable</strong></td>
					<td><strong>yes (critical)</strong></td>
			</tr>
			<tr>
					<td><strong>Rollback store</strong></td>
					<td>RWX PVC or S3 bucket</td>
					<td><strong>no when it is the only copy</strong></td>
					<td><strong>yes if <code>rollbackOnFailure</code> is used</strong></td>
			</tr>
			<tr>
					<td>Applied workloads</td>
					<td>stage sources</td>
					<td>yes — re-applied from sources</td>
					<td>no</td>
			</tr>
			<tr>
					<td>Webhook self-signed CA</td>
					<td>the controller</td>
					<td>yes — regenerated on startup</td>
					<td>no</td>
			</tr>
			<tr>
					<td>Leader lease</td>
					<td>the controller</td>
					<td>yes — re-elected</td>
					<td>no</td>
			</tr>
	</tbody>
</table>
<h2 id="what-to-back-up">What to back up</h2>
<ul>
<li><strong>Your GitOps repository.</strong> It holds every <code>StageSet</code> spec and the Flux source
objects (<code>GitRepository</code>, <code>OCIRepository</code>, <code>Bucket</code>, <code>ExternalArtifact</code>
producers) the stages reference. This is the primary source of truth — keep it
backed up the way you back up any Git repository.</li>
<li><strong><code>StageInventory</code> resources.</strong> These are cluster state, not in Git. Capture them
with a cluster backup tool (for example Velero) or an etcd snapshot. Because the
inventory deliberately lives in <code>spec</code> — not <code>status</code> — any backup tool that
restores <code>spec</code> captures the prune history. The relevant scope is every
<code>StageInventory</code> in each <code>StageSet</code>&rsquo;s namespace.</li>
<li><strong>Encryption key Secrets.</strong> Back up the Secrets named by
<code>spec.decryption.secretRef</code> in each <code>StageSet</code>&rsquo;s namespace. For a cloud-KMS-only
setup, back up the KMS key policy and ARN rather than key material.</li>
<li><strong>Rollback-store contents and credentials</strong>, if <code>spec.rollbackOnFailure: true</code>.
Snapshot the RWX volume or replicate the S3 bucket, and back up the bucket
credentials Secret. Skip this if every producer retains more than one revision —
rollback then re-fetches from the producer and the store is only a fast path.</li>
</ul>
<h2 id="rebuilding-a-cluster">Rebuilding a cluster</h2>
<p>Restore in the order below. The order matters: the encryption keys and the
inventory must exist <strong>before</strong> the controller reconciles a <code>StageSet</code>, or the first
reconcile does damage.</p>
<ol>
<li><strong>Install the controller</strong> from the Helm chart. See
<a href="/running/production/">Production</a>
 for the hardened install.</li>
<li><strong>Restore the encryption key Secrets.</strong> Keys must exist before any reconcile,
because a stage that decrypts SOPS files fails until its <code>secretRef</code> Secret is
present, and a <code>rollbackOnFailure</code> restore that re-decrypts a prior revision
fails with <code>PreviousRevisionUnavailable</code> without them. See
<a href="/security/encryption/">Secrets encryption</a>
.</li>
<li><strong>Restore the rollback-store credentials and contents</strong> if you use one, so a run
that fails immediately after rebuild can still restore its last-good output. See
<a href="/gating/rollback/">Rollback</a>
.</li>
<li><strong>Restore or re-sync the Flux sources.</strong> Let Flux reconcile the
<code>GitRepository</code> / <code>OCIRepository</code> / <code>Bucket</code> objects and the producers so each
stage&rsquo;s <code>ExternalArtifact</code> becomes <code>Ready</code> again.</li>
<li><strong>Restore the <code>StageSet</code> resources</strong> — normally by letting Flux apply them from
your GitOps repository.</li>
<li><strong>Restore the <code>StageInventory</code> resources</strong> before the controller reconciles the
<code>StageSet</code>s, so the first reconcile can prune correctly and a later deletion can
tear down. Restoring the captured <code>spec</code> is sufficient — that is where the prune
history lives. If a <code>StageSet</code> reconciles before its inventory is back, the run
applies and verifies but cannot prune; objects a newer revision dropped are left
orphaned until you restore the inventory and reconcile again.</li>
</ol>
<p>If you suspend each <code>StageSet</code> (<code>spec.suspend: true</code>) before step 5 and resume only
after step 6, you remove the race entirely.</p>
<h2 id="failure-modes">Failure modes</h2>
<table>
	<thead>
			<tr>
					<th>Lost item</th>
					<th>What happens</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>StageInventory</code></td>
					<td>the next reconcile applies and verifies, but the prune step has nothing to diff against, so removed objects are <strong>orphaned</strong>; <code>StageSet</code> deletion cannot reverse-order tear stages down</td>
			</tr>
			<tr>
					<td>Encryption key Secret</td>
					<td>new runs that decrypt SOPS files fail; a <code>rollbackOnFailure</code> restore that must re-decrypt fails with reason <code>PreviousRevisionUnavailable</code></td>
			</tr>
			<tr>
					<td>Rollback store</td>
					<td>a rollback falls back to the producer&rsquo;s retained revisions; with single-revision retention there is nothing to restore and the rollback fails</td>
			</tr>
	</tbody>
</table>
<h2 id="verifying-recovery">Verifying recovery</h2>
<p>After a rebuild, confirm the controller is back in a healthy steady state:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl"><span class="c1"># Every StageSet reports Ready=True with reason Succeeded.</span>
</span></span><span class="line"><span class="cl">kubectl --namespace payments get stagesets
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># The inventory is present for each stage.</span>
</span></span><span class="line"><span class="cl">kubectl --namespace payments get stageinventories
</span></span></code></pre></div><p>Then confirm pruning works end to end: change a stage&rsquo;s source so it renders one
fewer object, let the <code>StageSet</code> reconcile, and check the dropped object is gone. A
correct prune proves the restored inventory is intact — the controller&rsquo;s own
<a href="/contributing/building/">disaster-recovery test</a>
 pins exactly this guarantee: with
the inventory restored, a removed object is pruned; with the inventory lost, the
same removed object is orphaned.</p>
<h2 id="related">Related</h2>
<ul>
<li><a href="/defining-a-release/inventory/">Inventory and pruning</a>
 — what the <code>StageInventory</code> records and how the prune diff works.</li>
<li><a href="/security/encryption/">Secrets encryption</a>
 — how <code>spec.decryption.secretRef</code> keys are read and used.</li>
<li><a href="/gating/rollback/">Rollback</a>
 — the rollback store and <code>rollbackOnFailure</code> behavior.</li>
<li><a href="/running/production/">Production</a>
 — hardening a production install.</li>
<li><a href="/running/scale-and-capacity/">Scale and capacity</a>
 — sizing replicas and the state a multi-replica install keeps reachable.</li>
<li><a href="/runbooks/previousrevisionunavailable/"><code>PreviousRevisionUnavailable</code> runbook</a>
 — remediating a rollback that cannot restore.</li>
</ul>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/backup" term="backup" label="backup"/><category scheme="https://stageset.projects.metio.wtf/tags/disaster-recovery" term="disaster-recovery" label="disaster-recovery"/><category scheme="https://stageset.projects.metio.wtf/tags/restore" term="restore" label="restore"/><category scheme="https://stageset.projects.metio.wtf/tags/inventory" term="inventory" label="inventory"/><category scheme="https://stageset.projects.metio.wtf/tags/encryption" term="encryption" label="encryption"/><category scheme="https://stageset.projects.metio.wtf/tags/operations" term="operations" label="operations"/></entry><entry><title type="html">Operations</title><link href="https://stageset.projects.metio.wtf/running/operations/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/runbooks/reconcile-latency/?utm_source=atom_feed" rel="related" type="text/html" title="Reconcile latency high"/><link href="https://stageset.projects.metio.wtf/runbooks/workqueue-saturation/?utm_source=atom_feed" rel="related" type="text/html" title="Workqueue saturation"/><link href="https://stageset.projects.metio.wtf/observability/alerting/?utm_source=atom_feed" rel="related" type="text/html" title="Alerting"/><link href="https://stageset.projects.metio.wtf/runbooks/budgetsourceunavailable/?utm_source=atom_feed" rel="related" type="text/html" title="BudgetSourceUnavailable"/><id>https://stageset.projects.metio.wtf/running/operations/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Metrics, alerts, events, and runbooks for running the controller day to day.</blockquote><h2 id="metrics">Metrics</h2>
<p>The controller registers custom metrics on the controller-runtime registry, served
on <code>--metrics-bind-address</code> (<code>:8080</code>) alongside the standard
<code>controller_runtime_*</code> and <code>workqueue_*</code> series. Enable scraping with the chart&rsquo;s
opt-in <code>ServiceMonitor</code> (<code>metrics.serviceMonitor.enabled</code>):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># values.yaml</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">        </span><span class="c"># needs the Prometheus operator CRDs</span><span class="w">
</span></span></span></code></pre></div><table>
	<thead>
			<tr>
					<th>Metric</th>
					<th>Type</th>
					<th>Labels</th>
					<th>Meaning</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>stageset_reconcile_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>reason</code></td>
					<td>Reconciles, by terminal Ready reason.</td>
			</tr>
			<tr>
					<td><code>stageset_stage_applied_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Stages applied and verified.</td>
			</tr>
			<tr>
					<td><code>stageset_drift_corrected_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code>, <code>stage</code></td>
					<td>Out-of-band drift re-asserted on a steady-state reconcile.</td>
			</tr>
			<tr>
					<td><code>stageset_update_deferred_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code></td>
					<td>Rollouts held by a closed update window.</td>
			</tr>
			<tr>
					<td><code>stageset_webhook_cert_renewal_failures_total</code></td>
					<td>counter</td>
					<td><em>(none)</em></td>
					<td>Failed self-signed webhook cert renewals.</td>
			</tr>
			<tr>
					<td><code>stageset_teardown_force_drop_total</code></td>
					<td>counter</td>
					<td><code>namespace</code>, <code>name</code></td>
					<td>Finalizers force-dropped after <code>--max-teardown-wait</code> of failing teardown; sustained non-zero values flag an unreachable target and orphaned objects. See <a href="/runbooks/teardown-forced/">TeardownForced</a>
.</td>
			</tr>
			<tr>
					<td><code>stageset_stage_ready</code></td>
					<td>gauge</td>
					<td><code>namespace</code>, <code>stageset</code>, <code>stage</code></td>
					<td><code>1</code> when a stage is Ready, else <code>0</code> — for metric-based <a href="/guides/progressive-delivery/#argo-rollouts">progressive delivery</a>
.</td>
			</tr>
	</tbody>
</table>
<h2 id="alerts">Alerts</h2>
<p>The chart ships an opt-in <code>PrometheusRule</code> with a starter alert set, gated on
<code>metrics.prometheusRule.enabled</code> (requires the
<a href="https://prometheus-operator.dev/">Prometheus operator</a>
 CRDs). It covers the
custom <code>stageset_*</code> metrics plus controller-runtime signals:</p>
<table>
	<thead>
			<tr>
					<th>Alert</th>
					<th>Fires on</th>
					<th>Severity</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>StageSetReconcileErrorsHigh</code></td>
					<td>per-StageSet Ready=False rate (excludes the healthy <code>Succeeded</code>/<code>Suspended</code> reasons)</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetControllerWorkqueueDepthHigh</code></td>
					<td>the reconcile queue not draining</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetReconcileLatencyHigh</code></td>
					<td>reconcile p99 latency over threshold</td>
					<td>warning</td>
			</tr>
			<tr>
					<td><code>StageSetControllerPodDown</code></td>
					<td>a controller pod NotReady</td>
					<td>critical</td>
			</tr>
			<tr>
					<td><code>StageSetWebhookCertRenewalFailing</code></td>
					<td>self-signed cert rotation failing</td>
					<td>critical</td>
			</tr>
	</tbody>
</table>
<p>Every threshold is a knob under <code>metrics.prometheusRule.thresholds</code>, and
<code>extraAlertLabels</code> is merged onto every rendered alert so all stageset alerts can
route through one Alertmanager receiver. Each alert carries a <code>runbook_url</code>
annotation pointing at the matching <a href="/runbooks/">runbook</a>
 page on this site; the
URL prefix is fixed to this site, and the reconcile-errors alert templates the
URL on <code>$labels.reason</code>. Append your own rules under
<code>metrics.prometheusRule.extraRules</code>, and silence a built-in alert by raising its
threshold rather than forking the chart. When <code>StageSetControllerWorkqueueDepthHigh</code>
or <code>StageSetReconcileLatencyHigh</code> fires persistently, the controller is at capacity
— see <a href="/running/scale-and-capacity/">Scale and capacity</a>
 for which lever to
pull.</p>
<h2 id="events">Events</h2>
<p>The controller emits Kubernetes Events on every Ready-condition transition, so
<code>kubectl describe stageset &lt;name&gt;</code> and <a href="https://fluxcd.io/">Flux</a>
&rsquo;s
<code>notification-controller</code> (via an <code>Alert</code> targeting <code>kind: StageSet</code>) both
surface what happened. Normal events
include <code>Succeeded</code>, <code>UpdateDeferred</code>, <code>MigrationStarted</code>, and
<code>MigrationCompleted</code>; warnings include <code>StageFailed</code>, <code>DriftCorrected</code>,
<code>RolledBack</code>, <code>MigrationFailed</code>, <code>OnFailureAction</code>, <code>RollbackStoreFailed</code>, and
<code>TeardownForced</code> (a deleting StageSet&rsquo;s finalizer force-dropped after
<code>--max-teardown-wait</code> — see <a href="/runbooks/teardown-forced/">TeardownForced</a>
).</p>
<h2 id="runbooks">Runbooks</h2>
<p>Every actionable Ready-condition reason has a <a href="/runbooks/">runbook</a>
 covering the
symptom, cause, diagnosis, and remediation. The controller appends a
<code>(runbook: https://stageset.projects.metio.wtf/runbooks/&lt;reason&gt;/)</code> link to every
actionable Ready message (the reason lower-cased into a path segment), so a
<code>kubectl describe</code> links straight to the fix. Healthy reasons (<code>Succeeded</code>,
<code>Suspended</code>) get no link.</p>
<p>For example, a <code>StageFailed</code> StageSet shows:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">Message:  stage &#34;application&#34; failed: … (runbook: https://stageset.projects.metio.wtf/runbooks/stagefailed/)
</span></span></code></pre></div><h2 id="forcing-a-reconcile">Forcing a reconcile</h2>
<p>The controller reconciles on its <code>spec.interval</code>, on source changes, and on
demand. To trigger an out-of-band run, stamp the standard annotation — which is
what <code>flux reconcile</code> and <a href="/cli/reconcile/"><code>stagesetctl reconcile</code></a>
 do for you:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">kubectl annotate stageset my-app <span class="se">\
</span></span></span><span class="line"><span class="cl">  reconcile.fluxcd.io/requestedAt<span class="o">=</span><span class="s2">&#34;</span><span class="k">$(</span>date -u +%FT%TZ<span class="k">)</span><span class="s2">&#34;</span> --overwrite
</span></span></code></pre></div><p>The handled token is recorded in <code>status.lastHandledReconcileAt</code>.</p>
<h2 id="drift-correction">Drift correction</h2>
<p>On a steady-state reconcile the controller re-asserts the desired state, healing
out-of-band changes to managed objects. Each correction emits a <code>DriftCorrected</code>
event and increments <code>stageset_drift_corrected_total</code>. Tighten the cadence with
<code>spec.driftDetectionInterval</code> when you need faster healing than <code>spec.interval</code>.</p>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/operations" term="operations" label="operations"/><category scheme="https://stageset.projects.metio.wtf/tags/metrics" term="metrics" label="metrics"/><category scheme="https://stageset.projects.metio.wtf/tags/alerts" term="alerts" label="alerts"/><category scheme="https://stageset.projects.metio.wtf/tags/runbooks" term="runbooks" label="runbooks"/></entry><entry><title type="html">Production</title><link href="https://stageset.projects.metio.wtf/running/production/?utm_source=atom_feed" rel="alternate" type="text/html"/><link href="https://stageset.projects.metio.wtf/runbooks/webhook-cert-renewal/?utm_source=atom_feed" rel="related" type="text/html" title="Webhook cert renewal failing"/><link href="https://stageset.projects.metio.wtf/security/admission-webhook/?utm_source=atom_feed" rel="related" type="text/html" title="Admission webhook"/><link href="https://stageset.projects.metio.wtf/running/disaster-recovery/?utm_source=atom_feed" rel="related" type="text/html" title="Backup and disaster recovery"/><link href="https://stageset.projects.metio.wtf/runbooks/controller-pod-down/?utm_source=atom_feed" rel="related" type="text/html" title="Controller pod down"/><link href="https://stageset.projects.metio.wtf/runbooks/migrationsourcenotpinned/?utm_source=atom_feed" rel="related" type="text/html" title="MigrationSourceNotPinned"/><id>https://stageset.projects.metio.wtf/running/production/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>A decision-oriented checklist for hardening a stageset-controller install before it manages production releases.</blockquote><p>The chart&rsquo;s defaults bring up a hardened, lease-guarded controller, but a few
decisions still belong to you before it manages production releases: whether
rollback is enabled and where its store lives, how many replicas run, what the
controller can reach, and how you observe and recover it. Work through these in
order. Each step states what to set and why, and ends with the Helm values for
that step. Every flag and its Helm value live in the <a href="/reference/configuration/">configuration
reference</a>
.</p>
<h2 id="1-decide-on-rollback-and-pick-its-store">1. Decide on rollback and pick its store</h2>
<p>Rollback is opt-in. With no store the controller leaves a failed run on the
broken release; with one it restores the last successfully-applied artifact
revisions. The store keeps bit-exact copies that outlive the producer&rsquo;s garbage
collection, so it is the difference between a recoverable failure and a manual
incident.</p>
<p>Two mutually-exclusive backends are available — a <code>ReadWriteMany</code> PVC or an
S3-compatible bucket. For HA the store must be reachable from whichever pod holds
the lease, which rules out a <code>ReadWriteOnce</code> PVC. On-prem, use an RWX class (NFS,
CephFS); on a cloud provider, use the object store. The decision, the keep-count,
and how restore selects a revision are covered in <a href="/gating/rollback/">Rollback</a>
.</p>
<p>The store holds rendered <code>Secret</code> data, so encrypt it at rest. The S3 backend
takes a server-side-encryption mode (SSE-S3 or SSE-KMS with your key); see
<a href="/gating/rollback/#encryption-at-rest">encryption at rest</a>
. This is distinct from
SOPS decryption of a stage&rsquo;s own source, which is configured per stage in
<a href="/security/encryption/">Secrets encryption</a>
.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Option A: RWX PVC (on-prem)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rollbackStore</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">backend</span><span class="p">:</span><span class="w"> </span><span class="l">pvc</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">pvc</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">accessModes</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">ReadWriteMany]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">storageClass</span><span class="p">:</span><span class="w"> </span><span class="l">nfs-client    </span><span class="w"> </span><span class="c"># your RWX class (NFS, CephFS, …)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">size</span><span class="p">:</span><span class="w"> </span><span class="l">10Gi</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># Option B: S3-compatible bucket (cloud)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rollbackStore</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">backend</span><span class="p">:</span><span class="w"> </span><span class="l">s3</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">s3</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">endpoint</span><span class="p">:</span><span class="w"> </span><span class="l">s3.eu-west-1.amazonaws.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">bucket</span><span class="p">:</span><span class="w"> </span><span class="l">my-org-stageset-rollback</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">region</span><span class="p">:</span><span class="w"> </span><span class="l">eu-west-1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">sse</span><span class="p">:</span><span class="w"> </span><span class="l">s3                     </span><span class="w"> </span><span class="c"># SSE-S3; or kms (+ sseKmsKeyId), or none</span><span class="w">
</span></span></span></code></pre></div><h2 id="high-availability">2. Run leader-elected HA</h2>
<p>The chart enables leader election by default, so even a single replica is
lease-guarded. For HA, raise the replica floor above one: only the lease holder
reconciles, while every replica answers admission webhook calls, so admission
stays available during a leader handover. When the replica ceiling exceeds the
floor the chart also renders a <code>HorizontalPodAutoscaler</code> (CPU target 80%) and a
<code>PodDisruptionBudget</code> (<code>minAvailable: 1</code>).</p>
<p>The lease is not released eagerly on shutdown, so after a rolling update the new
leader takes over when the old lease expires — budget a few seconds of reconcile
pause on restart. Admission and the gate endpoint stay available throughout,
since every replica serves them. The HA model is detailed in
<a href="/security/multi-cluster/">multi-cluster and tenancy</a>
.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">replicas</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">min</span><span class="p">:</span><span class="w"> </span><span class="m">2</span><span class="w">                 </span><span class="c"># leader-elected HA; the non-leader still serves admission</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">max</span><span class="p">:</span><span class="w"> </span><span class="m">3</span><span class="w">                 </span><span class="c"># &gt; min renders an HPA (CPU 80%) and a PodDisruptionBudget</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">leaderElect</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><h2 id="3-choose-the-tenancy-model-and-scope-rbac">3. Choose the tenancy model and scope RBAC</h2>
<p>The controller never applies your manifests as itself. Every cluster operation
for a <code>StageSet</code> — building, applying, pruning, running actions — runs as the
<code>StageSet</code>&rsquo;s <code>spec.serviceAccountName</code>, so a release can only do what its tenant
<code>ServiceAccount</code> permits and an over-broad or missing SA fails closed. On the
local cluster the controller assumes that identity by minting a short-lived
TokenRequest token for the SA, so the chart grants it only <code>create</code> on
<code>serviceaccounts/token</code> — not write access and not the <code>impersonate</code> verb. Give
every production <code>StageSet</code> a scoped <code>ServiceAccount</code>.</p>
<p>Pick the model that matches your cluster. Multi-tenant clusters keep
impersonation on and may scope the controller to a namespace set — one instance
per tenant group, which also pivots RBAC to per-namespace bindings. Single-tenant
clusters can bind the controller to <code>cluster-admin</code> so StageSets apply without a
<code>serviceAccountName</code>. Both models, and remote-cluster reconciliation, are in
<a href="/security/multi-cluster/">multi-cluster and tenancy</a>
. To hard-isolate namespaces,
deny cross-namespace <code>sourceRef</code>/<code>dependsOn</code> references.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Multi-tenant: scope the controller to a namespace set (RBAC pivots to</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># per-namespace bindings) and hard-isolate cross-namespace references.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">watchNamespaces</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">team-a, team-b]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">noCrossNamespaceRefs</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># Single-tenant: bind the controller SA to cluster-admin so StageSets apply</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># without a per-release serviceAccountName.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rbac</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">clusterAdmin</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><h2 id="4-set-resources-for-your-release-sizes">4. Set resources for your release sizes</h2>
<p>The chart constrains the pod with requests equal to limits, so it is fully
bounded out of the box. The defaults suit ordinary workloads; raise them for very
large inventories or very busy clusters. Ephemeral storage covers <code>/tmp</code> and the
self-signed cert directory, both emptyDirs. The reconcile cadence StageSets
inherit when they omit <code>spec.interval</code>, and the inventory mode and shard cap that
govern how applied objects are tracked, are tuning knobs worth reviewing.
<a href="/running/scale-and-capacity/">Scale and capacity</a>
 covers the sizing drivers
and these throughput knobs in depth.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">resources</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">cpu</span><span class="p">:</span><span class="w"> </span><span class="l">50m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">memory</span><span class="p">:</span><span class="w"> </span><span class="l">256Mi</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">ephemeralStorage</span><span class="p">:</span><span class="w"> </span><span class="l">32Mi  </span><span class="w"> </span><span class="c"># /tmp and the self-signed cert dir are emptyDirs</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">defaultInterval</span><span class="p">:</span><span class="w"> </span><span class="l">10m    </span><span class="w"> </span><span class="c"># cadence StageSets inherit when they omit spec.interval</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">inventoryShardCap</span><span class="p">:</span><span class="w"> </span><span class="m">5000</span><span class="w">  </span><span class="c"># lower for smaller inventory objects on huge applies</span><span class="w">
</span></span></span></code></pre></div><h2 id="network-policy">5. Fence the network and the gate</h2>
<p>The gate endpoint is unauthenticated — a read-only <code>GET</code> that progressive-delivery
tooling (Flagger, Argo Rollouts) polls. Turn on the ingress-only <code>NetworkPolicy</code>
to fence the gate, webhook, and metrics ports to only the peers that need them.</p>
<p>The policy is ingress-only and does not restrict egress. The controller still
fetches stage artifacts over HTTP from source-controller, so if your cluster
default-denies egress, add an egress allowance to source-controller and DNS. When
a stage runs <code>http</code> <a href="/defining-a-release/actions/">actions</a>
, the hosts those actions may reach
are an explicit allow-list — loopback and link-local are always denied.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">              </span><span class="c"># admits the webhook (9443), metrics (8080), gate (8082)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">allowedActionHosts</span><span class="p">:</span><span class="w">        </span><span class="c"># host globs http actions may reach; loopback/link-local always denied</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="s2">&#34;*.internal.example.com&#34;</span><span class="w">
</span></span></span></code></pre></div><h2 id="admission-webhook-tls">6. Provision the admission webhook certificate</h2>
<p>The webhook validates <code>StageSet</code> spec invariants at <code>kubectl apply</code> time instead
of at reconcile time. It is wired by the chart; the one decision is how its
serving certificate is obtained. The default <code>self-signed</code> mode generates an
in-pod CA and serving cert and rotates them at a third of their validity with no
external dependency. The <code>cert-manager</code> mode hands issuance and rotation to
<a href="https://cert-manager.io/">cert-manager</a>
, which must be installed in the cluster.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Option A: cert-manager (issues + rotates the cert; requires cert-manager)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certMode</span><span class="p">:</span><span class="w"> </span><span class="l">cert-manager</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certManager</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">issuerRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ClusterIssuer</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">letsencrypt-prod</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># Option B: self-signed (chart default: in-pod CA + serving cert, rotated at</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="c"># validity/3, with no cert-manager dependency)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certMode</span><span class="p">:</span><span class="w"> </span><span class="l">self-signed</span><span class="w">
</span></span></span></code></pre></div><h2 id="7-enable-observability-and-alerts">7. Enable observability and alerts</h2>
<p>Turn on the <code>ServiceMonitor</code> if you scrape with the Prometheus operator, and the
bundled <code>PrometheusRule</code> for the starter alert set. Match the Prometheus selector
labels so your instance picks up each object. The exposed metrics, the shipped
alerts, and the events the controller emits are documented in
<a href="/running/operations/#metrics">Operations</a>
.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">additionalLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">release</span><span class="p">:</span><span class="w"> </span><span class="l">kube-prom      </span><span class="w"> </span><span class="c"># match your Prometheus&#39;s serviceMonitorSelector</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">prometheusRule</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">release</span><span class="p">:</span><span class="w"> </span><span class="l">kube-prom      </span><span class="w"> </span><span class="c"># match your Prometheus&#39;s ruleSelector</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">extraAlertLabels</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">team</span><span class="p">:</span><span class="w"> </span><span class="l">platform          </span><span class="w"> </span><span class="c"># Alertmanager routing label</span><span class="w">
</span></span></span></code></pre></div><p>Every actionable Ready-condition reason has a <a href="/runbooks/">runbook</a>
, and the
controller appends a direct link to each Ready message
(<code>(runbook: https://stageset.projects.metio.wtf/runbooks/&lt;reason&gt;/)</code>), so a
<code>kubectl describe</code> on a failing StageSet routes straight to the fix. Healthy
reasons get no link.</p>
<h2 id="8-plan-for-upgrades-and-recovery">8. Plan for upgrades and recovery</h2>
<p>Calendar-based releases run on a weekly cron. Chart upgrades are <code>helm upgrade --install</code>; the chart ships CRDs under <code>templates/</code>, so schema changes apply
automatically. Read
<a href="/reference/upgrading/">Upgrading</a>

before each upgrade — a release that changes an immutable selector field requires
a manual delete first. For day-two work — forcing a reconcile, drift correction,
reading events, and following a runbook — see
<a href="/running/operations/">Operations</a>
.</p>
<h2 id="a-complete-production-valuesyaml">A complete production values.yaml</h2>
<p>Two HA setups share one backbone: a leader-elected pair (or trio), a rollback
store reachable from whichever pod holds the lease, cert-manager for the webhook,
a <code>NetworkPolicy</code> fencing the unauthenticated gate, and a <code>ServiceMonitor</code> for
Prometheus. They differ only in where the rollback store lives.</p>
<h3 id="on-prem-rwx-storage">On-prem (RWX storage)</h3>
<p>Back the store with a <code>ReadWriteMany</code> PVC on your storage class (NFS, CephFS, …)
so every replica mounts the same volume — the store must be reachable from the
lease holder, and an RWO PVC cannot satisfy that under HA.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># values-onprem.yaml</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">replicas</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">min</span><span class="p">:</span><span class="w"> </span><span class="m">2</span><span class="w">                 </span><span class="c"># leader-elected HA; the non-leader still serves admission</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">max</span><span class="p">:</span><span class="w"> </span><span class="m">3</span><span class="w">                 </span><span class="c"># &gt; min renders an HPA (CPU 80%) and a PodDisruptionBudget</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">leaderElect</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rollbackStore</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">backend</span><span class="p">:</span><span class="w"> </span><span class="l">pvc</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">pvc</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">accessModes</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="l">ReadWriteMany]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">storageClass</span><span class="p">:</span><span class="w"> </span><span class="l">nfs-client    </span><span class="w"> </span><span class="c"># your RWX class (NFS, CephFS, …)</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">size</span><span class="p">:</span><span class="w"> </span><span class="l">10Gi</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certMode</span><span class="p">:</span><span class="w"> </span><span class="l">cert-manager        </span><span class="w"> </span><span class="c"># requires cert-manager in the cluster</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">                  </span><span class="c"># fences the unauthenticated gate endpoint</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">helm upgrade --install stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  oci://ghcr.io/metio/helm-charts/stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  --namespace stageset-system --create-namespace <span class="se">\
</span></span></span><span class="line"><span class="cl">  -f values-onprem.yaml
</span></span></code></pre></div><h3 id="aws--eks-s3--irsa">AWS / EKS (S3 + IRSA)</h3>
<p>Back the store with S3 and let the controller assume an IAM role through
<a href="https://docs.aws.amazon.com/eks/latest/userguide/iam-roles-for-service-accounts.html">IRSA</a>

instead of static keys: annotate the controller&rsquo;s ServiceAccount with the role
ARN and leave the S3 credentials empty, and the store&rsquo;s minio-go client picks the
role up from the pod&rsquo;s web-identity token. Grant the role
<code>s3:GetObject</code>/<code>PutObject</code>/<code>ListBucket</code>/<code>DeleteObject</code> on the bucket.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># values-eks.yaml</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">replicas</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">min</span><span class="p">:</span><span class="w"> </span><span class="m">2</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">max</span><span class="p">:</span><span class="w"> </span><span class="m">3</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">leaderElect</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">serviceAccount</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">annotations</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># an IAM role granting s3:GetObject/PutObject/ListBucket/DeleteObject on the bucket</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">eks.amazonaws.com/role-arn</span><span class="p">:</span><span class="w"> </span><span class="l">arn:aws:iam::123456789012:role/stageset-controller</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">rollbackStore</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">backend</span><span class="p">:</span><span class="w"> </span><span class="l">s3</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">s3</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">endpoint</span><span class="p">:</span><span class="w"> </span><span class="l">s3.eu-west-1.amazonaws.com</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">bucket</span><span class="p">:</span><span class="w"> </span><span class="l">my-org-stageset-rollback</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">region</span><span class="p">:</span><span class="w"> </span><span class="l">eu-west-1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">sse</span><span class="p">:</span><span class="w"> </span><span class="l">s3</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="c"># no existingSecret → credentials come from the IRSA role above</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">webhook</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">certMode</span><span class="p">:</span><span class="w"> </span><span class="l">cert-manager</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">networkPolicy</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metrics</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">serviceMonitor</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">helm upgrade --install stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  oci://ghcr.io/metio/helm-charts/stageset-controller <span class="se">\
</span></span></span><span class="line"><span class="cl">  --namespace stageset-system --create-namespace <span class="se">\
</span></span></span><span class="line"><span class="cl">  -f values-eks.yaml
</span></span></code></pre></div><h2 id="next-steps">Next steps</h2>
<ul>
<li><a href="/reference/configuration/">Configuration reference</a>
 — every flag, its
default, and the Helm value that drives it.</li>
<li><a href="/running/operations/">Operations</a>
 — metrics, alerts, events, forcing a
reconcile, drift correction.</li>
<li><a href="/gating/rollback/">Rollback</a>
 — store backends, keep-count, and revision
selection.</li>
<li><a href="/security/multi-cluster/">Multi-cluster and tenancy</a>
 — tenant ServiceAccounts,
namespace scoping, remote-cluster reconciliation.</li>
<li><a href="/runbooks/">Runbooks</a>
 — incident response.</li>
</ul>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/production" term="production" label="production"/><category scheme="https://stageset.projects.metio.wtf/tags/security" term="security" label="security"/><category scheme="https://stageset.projects.metio.wtf/tags/operations" term="operations" label="operations"/><category scheme="https://stageset.projects.metio.wtf/tags/ha" term="ha" label="ha"/></entry><entry><title type="html">Scale and capacity</title><link href="https://stageset.projects.metio.wtf/running/scale-and-capacity/?utm_source=atom_feed" rel="alternate" type="text/html"/><id>https://stageset.projects.metio.wtf/running/scale-and-capacity/</id><published>0001-01-01T00:00:00+00:00</published><updated>2026-06-26T15:53:52+02:00</updated><content type="html"><![CDATA[<blockquote>Size a controller replica for your workload, decide how many replicas to run, tune the throughput knobs, and read the metrics that tell you when you are at capacity.</blockquote><p>Size the controller for the number and size of the <code>StageSet</code>s it manages, then
tune its cadence and inventory knobs to fit. Start from the chart&rsquo;s defaults,
raise resources only where a driver below applies to your release sizes, and watch
the saturation signals so you scale on evidence rather than guesswork.</p>
<h2 id="sizing-a-replica">Sizing a replica</h2>
<p>A reconcile fetches each stage&rsquo;s artifact, decrypts and builds it, applies the
rendered objects server-side, and polls readiness until the stage&rsquo;s gates pass.
CPU and memory scale with different parts of that pipeline.</p>
<h3 id="cpu">CPU</h3>
<p>CPU is consumed by the build (kustomize render), the server-side apply of every
object, and the readiness polling that gates progression. A <code>StageSet</code> with many
stages, many objects per stage, or long-running readiness gates keeps a worker
busy longer. Because reconciles for distinct <code>StageSet</code>s are processed one at a
time (see <a href="#tuning-throughput">Tuning throughput</a>
), CPU pressure shows up as a
deeper workqueue and longer reconcile latency rather than as a pegged core. The
chart&rsquo;s default request of <code>50m</code> suits ordinary workloads; raise it when reconcile
latency climbs on a busy cluster.</p>
<h3 id="memory">Memory</h3>
<p>Resident memory is dominated by artifact fetching and the in-memory inventory. The
fetcher holds a stage&rsquo;s artifact in memory while it builds, and four independent
byte caps bound the worst case of a single fetch — they are internal constants,
not flags:</p>
<table>
	<thead>
			<tr>
					<th>Cap</th>
					<th>Limit</th>
					<th>Bounds</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>MaxArchiveBytes</code></td>
					<td>64 MiB</td>
					<td>the compressed download body</td>
			</tr>
			<tr>
					<td><code>MaxDecompressedBytes</code></td>
					<td>512 MiB</td>
					<td>the inflated gzip stream as it is read</td>
			</tr>
			<tr>
					<td><code>MaxPerEntryBytes</code></td>
					<td>16 MiB</td>
					<td>any single tar entry</td>
			</tr>
			<tr>
					<td><code>MaxExtractedBytes</code></td>
					<td>64 MiB</td>
					<td>the extracted result held in memory</td>
			</tr>
	</tbody>
</table>
<p>The extracted result a build sees is therefore capped at 64 MiB per stage, and the
gzip stream cannot inflate past 512 MiB even from a crafted archive — together
these defend against gzip bombs and cap the per-fetch footprint. The sharded
<code>StageInventory</code> adds memory proportional to how many objects each stage owns; very
large stages are split across shards (see
<a href="#tuning-throughput"><code>--inventory-shard-cap</code></a>
), so no single inventory object grows
unbounded.</p>
<p>The chart&rsquo;s default request of <code>256Mi</code> covers ordinary inventories. Raise it for
clusters with very large stages (many thousands of applied objects) or large
artifacts. A defensible starting point for a busy cluster managing large releases:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">resources</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">cpu</span><span class="p">:</span><span class="w"> </span><span class="l">200m</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">memory</span><span class="p">:</span><span class="w"> </span><span class="l">512Mi</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">ephemeralStorage</span><span class="p">:</span><span class="w"> </span><span class="l">64Mi  </span><span class="w"> </span><span class="c"># /tmp and the self-signed cert dir are emptyDirs</span><span class="w">
</span></span></span></code></pre></div><p>The chart sets requests equal to limits, so the pod is fully bounded; see
<a href="/running/production/#high-availability">Production</a>
 for the resource block in
context.</p>
<h2 id="replicas-and-ha">Replicas and HA</h2>
<p>Reconciliation is single-leader. The chart enables leader election by default, so
even a single replica is lease-guarded, and only the lease holder reconciles —
adding replicas does <strong>not</strong> add reconcile parallelism. What extra replicas buy is
<strong>failover</strong> and <strong>admission availability</strong>: every replica answers the validating
webhook and the read-only gate endpoint, so a <code>kubectl apply</code> of a <code>StageSet</code> and a
Flagger gate check stay served during a leader handover.</p>
<p>The lease is not released eagerly on shutdown, so after a rolling update the new
leader takes over only when the old lease expires — budget a few seconds of
reconcile pause on restart. Admission and the gate endpoint stay available
throughout.</p>
<p>Running more than one replica adds one hard requirement: the optional
<a href="/gating/rollback/">rollback store</a>
 must be reachable from whichever pod holds the
lease. A <code>ReadWriteOnce</code> PVC cannot satisfy that, so a multi-replica install needs
an <strong>RWX <code>PersistentVolume</code></strong> or an <strong>S3-compatible bucket</strong>. The
<a href="/running/production/#high-availability">Production</a>
 page has the full HA
setup, including the <code>HorizontalPodAutoscaler</code> and <code>PodDisruptionBudget</code> the chart
renders when the replica ceiling exceeds the floor; the leader model is detailed in
<a href="/security/multi-cluster/">multi-cluster and tenancy</a>
.</p>
<h2 id="tuning-throughput">Tuning throughput</h2>
<p>The knobs below control how often the controller reconciles and how it stores what
it applies. Each lists what it does, its default, when to change it, and the metric
that signals the need.</p>
<h3 id="reconcile-cadence----default-interval-and-specinterval">Reconcile cadence — <code>--default-interval</code> and <code>spec.interval</code></h3>
<p><code>--default-interval</code> (default <code>10m</code>) is the reconcile cadence a <code>StageSet</code> inherits
when it omits <code>spec.interval</code>. A source change reconciles immediately through the
artifact watch regardless of the interval; the interval is the periodic re-assert
that heals out-of-band drift. Lowering it makes the controller re-check applied
state more often — more responsive drift correction at the cost of more apiserver
and apply traffic per <code>StageSet</code>. A <code>StageSet</code> can override the global default per
release:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">stages.metio.wtf/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">StageSet</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">metadata</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">namespace</span><span class="p">:</span><span class="w"> </span><span class="l">payments</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">spec</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">interval</span><span class="p">:</span><span class="w"> </span><span class="l">5m                 </span><span class="w"> </span><span class="c"># re-assert applied state every 5 minutes</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">retryInterval</span><span class="p">:</span><span class="w"> </span><span class="l">1m            </span><span class="w"> </span><span class="c"># back off faster after a failed run</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">driftDetectionInterval</span><span class="p">:</span><span class="w"> </span><span class="l">30m  </span><span class="w"> </span><span class="c"># cheap drift sweep between full reconciles</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">stages</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">api</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">sourceRef</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">apiVersion</span><span class="p">:</span><span class="w"> </span><span class="l">source.toolkit.fluxcd.io/v1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">kind</span><span class="p">:</span><span class="w"> </span><span class="l">ExternalArtifact</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">payments-api</span><span class="w">
</span></span></span></code></pre></div><p><code>spec.driftDetectionInterval</code>, when set, re-asserts applied state on its own cadence
between full reconciles, so you can keep the full <code>interval</code> long while still
healing drift cheaply. Watch <code>stageset_drift_corrected_total</code> to judge whether
drift is frequent enough to warrant a shorter sweep.</p>
<h3 id="inventory-shard-size----inventory-shard-cap">Inventory shard size — <code>--inventory-shard-cap</code></h3>
<p><code>--inventory-shard-cap</code> (default <code>5000</code>) is the maximum number of entries a single
<code>StageInventory</code> object holds. When a stage owns more objects than the cap, its
inventory is split across multiple shards so no one object grows unbounded.
Sharding is automatic — the cap is a tuning knob you rarely touch. Lower it if your
apiserver struggles with large <code>StageInventory</code> objects on very large applies;
raise it to keep a moderately large stage in a single object. The per-stage shard
count surfaces on the <code>StageSet</code> status, and the mechanism is described in
<a href="/defining-a-release/inventory/">Inventory and pruning</a>
.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Helm values — controller-wide tuning.</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">controller</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">defaultInterval</span><span class="p">:</span><span class="w"> </span><span class="l">10m    </span><span class="w"> </span><span class="c"># cadence StageSets inherit when they omit spec.interval</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">inventoryShardCap</span><span class="p">:</span><span class="w"> </span><span class="m">5000</span><span class="w">  </span><span class="c"># lower for smaller inventory objects on huge applies</span><span class="w">
</span></span></span></code></pre></div><h3 id="watch-scope-and-reference-isolation">Watch scope and reference isolation</h3>
<p><code>--watch-namespaces</code> (empty by default, meaning cluster-wide) scopes the manager&rsquo;s
informers to a namespace set, so one deployment per tenant group observes only its
own <code>StageSet</code>s — the multi-tenant controller-instances pattern. A scoped instance
caches fewer objects and reconciles fewer <code>StageSet</code>s, which is both an isolation
and a capacity lever. <code>--no-cross-namespace-refs</code> hard-denies cross-namespace
<code>sourceRef</code> and <code>dependsOn</code> references. Both are covered in
<a href="/security/multi-cluster/">multi-cluster and tenancy</a>
.</p>
<h2 id="knowing-when-youre-at-capacity">Knowing when you&rsquo;re at capacity</h2>
<p>Scale on the saturation signals the controller exports, not on intuition. The
<a href="/observability/metrics/">metrics</a>
 page lists every series; the ones that mean &ldquo;at
capacity&rdquo; are:</p>
<ul>
<li><strong><code>workqueue_depth{controller=&quot;stageset&quot;}</code></strong> — a depth that stays high means
reconciles are arriving faster than the single-leader worker drains them. The
<code>StageSetControllerWorkqueueDepthHigh</code> alert fires past a sustained depth; its
runbook is <a href="/runbooks/workqueue-saturation/">workqueue saturation</a>
.</li>
<li><strong><code>controller_runtime_reconcile_time_seconds</code></strong> — rising p99 reconcile latency
points at CPU pressure or slow readiness gates. The
<code>StageSetReconcileLatencyHigh</code> alert covers it; see the
<a href="/runbooks/reconcile-latency/">reconcile latency runbook</a>
.</li>
<li><strong><code>stageset_reconcile_total{reason=…}</code></strong> — a climbing non-<code>Succeeded</code> rate is a
correctness or RBAC problem, not a sizing one; the
<code>StageSetReconcileErrorsHigh</code> alert tracks it.</li>
<li><strong><code>stageset_drift_corrected_total</code></strong> and <strong><code>stageset_update_deferred_total</code></strong> —
frequent drift correction argues for a <code>driftDetectionInterval</code>; frequent
deferrals mean update windows are throttling delivery.</li>
</ul>
<p>The chart ships these as a <code>PrometheusRule</code> with tunable thresholds — see
<a href="/observability/alerting/">Alerting</a>
 — and the published
<a href="/observability/dashboard/">dashboard</a>
 and <a href="/observability/slos/">SLOs</a>
 render the
same signals. When the workqueue stays deep and latency is high while reconciles
still succeed, the lever is a larger CPU/memory request on the replica, a longer
<code>--default-interval</code>, or splitting the workload across watch-scoped instances —
adding replicas alone will not help, because only the leader reconciles.</p>
<h2 id="related">Related</h2>
<ul>
<li><a href="/running/production/#high-availability">Production</a>
 — the hardened install, HA replicas, and the resource block in context.</li>
<li><a href="/defining-a-release/inventory/">Inventory and pruning</a>
 — sharding and the <code>StageInventory</code> the shard cap governs.</li>
<li><a href="/security/multi-cluster/">Multi-cluster and tenancy</a>
 — watch-scoped instances, the leader model, and cross-namespace isolation.</li>
<li><a href="/observability/metrics/">Metrics</a>
 — every exported series and how to scrape them.</li>
<li><a href="/observability/alerting/">Alerting</a>
 — the shipped saturation alerts and their thresholds.</li>
<li><a href="/running/disaster-recovery/">Backup and disaster recovery</a>
 — the state a multi-replica install must keep reachable.</li>
</ul>
]]></content><category scheme="https://stageset.projects.metio.wtf/tags/scale" term="scale" label="scale"/><category scheme="https://stageset.projects.metio.wtf/tags/capacity" term="capacity" label="capacity"/><category scheme="https://stageset.projects.metio.wtf/tags/sizing" term="sizing" label="sizing"/><category scheme="https://stageset.projects.metio.wtf/tags/resources" term="resources" label="resources"/><category scheme="https://stageset.projects.metio.wtf/tags/leader-election" term="leader-election" label="leader-election"/><category scheme="https://stageset.projects.metio.wtf/tags/throughput" term="throughput" label="throughput"/><category scheme="https://stageset.projects.metio.wtf/tags/tuning" term="tuning" label="tuning"/></entry></feed>